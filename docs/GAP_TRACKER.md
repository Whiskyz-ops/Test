# Coverage Gap Tracker

Living document tracking everything WISING does **not** yet model, viewed through three separate lenses — **India-only** (a taxpayer living solely in India with only Indian income), **US-only** (solely US), and **cross-border** — so domestic accuracy is tracked with the same rigor as treaty work.

**Last verified against live sources: 11 July 2026.** Items marked "verified" were checked by web research on that date, not recalled from memory. Re-verify time-sensitive rows (rates, thresholds, treaty status) every quarter and after every Union Budget / US tax act.

**Status legend:** ✅ modeled · 🟡 partial · ❌ missing · 🔍 needs code verification · 🚫 out of scope by design

**Build Now? legend** (added per request — divides every row by whether *I* can build it without waiting on anything):
- 🟢 **Now** — every input already exists somewhere in Layer 1 (India or US); this is engine/UI work only, no form changes needed. Where only *part* of a row is buildable now, the Detail column says so explicitly.
- 🟡 **Needs field** — genuinely blocked until a new Layer 1 field is added (via an Antigravity prompt or direct edit) and round-tripped back.
- ⚪ **N/A** — doesn't fit the Now/Needs-field frame: already in flight with a prompt issued, a recorded out-of-scope decision, or needs a Layer 1 coverage check before it can even be classified.

**In-product surfacing:** every 🚫/assurance row that is not rule-encodable (GAAR, STT, payer-side TDS, clubbing depth, FICA levy, FATCA Ch.4, mocked uploads, plus the MLI/DTAA assurances) is ALSO shown inside the Monitor itself — the "Deliberately out of scope" card at the bottom of the overview, built by `buildScopeNotes()` in conflicts.js and gated per profile (e.g. the payer-TDS note only appears for business profiles, FICA only when US wages/SE income exist). The tracker records the decision; the Monitor discloses it to the professional.

**Priority legend:** P1 = real money exposure computable or misstatement risk now · P2 = valuable, needs new Layer 1 fields or moderate build · P3 = completeness/edge audience

---

## A. India-only lens (domestic taxpayer, no US exposure)

| ID | Item | Status | Build Now? | Priority | Detail |
|---|---|---|---|---|---|
| IN-1 | ~~Advance-tax / late-filing interest — ss.423/424/425 (old 234A/B/C)~~ **FIXED (s.424/425)** | ✅ shipped (partial) | — | — | User-directed, after the two single-jurisdiction demo profiles (IN/US-only) exposed how few real compliance-risk findings existed for a domestic-only taxpayer. s.424 (234B, default in paying advance tax): if advance tax paid < 90% of assessed tax (total liability less TDS/TCS), interest = shortfall × 1%/month × months from 1 Apr to the statutory filing due date (4 months non-audit/31 Jul, 7 months audit-case/31 Oct — same s.44AB turnover-threshold test `monitoring.js`'s Compliance Calendar already uses, kept in sync deliberately rather than shared). s.425 (234C, installment deferment): each quarter's own required slice (15/30/30/25% of assessed tax) tested independently against what was actually paid THAT quarter (Layer 1's `advance_tax_q1..q4` fields are per-quarter amounts, not cumulative) — 3 months' interest for a Q1-Q3 shortfall, 1 month for Q4, no cross-quarter netting of a later overpayment against an earlier shortfall (matches the real provision). **s.423 (234A, late RETURN filing) NOT built** — needs the actual return-FILING date, which this single-year-snapshot tool only ever has a statutory DUE date for, never an actual-filed date to measure from; documented as the reason IN-1 stays "partial" rather than fully closing. Known simplification: s.425's narrow income-timing relief (capital gains/lottery/dividends genuinely arising after a quarter's due date) isn't modeled, since Layer 1 doesn't capture per-transaction dates at that granularity — can overstate the estimate for late-year income. Verified: 27-test suite; hand-checked exact (₹1,91,735, matching the formula to the rupee) against `india_only_ca_client`; sane, real, previously-invisible numbers surfaced across 7 of the 9 pre-existing demo profiles too (₹7,610 to ₹4,14,168) — most had never been tuned for adequate advance-tax coverage since this finding never existed to check it. **Refined 16 Jul 2026 (web-verification pass):** s.425's presumptive proviso now applied — a business that is ONLY presumptive (s.58, old 44AD/ADA; no regular-books entry, no partner-firm PGBP) owes a single 100% installment by 15 Mar (1 month of interest on a shortfall) instead of the quarterly ladder, and the Compliance Calendar shows that single installment in place of the four quarterly rows. Mixed presumptive+regular profiles conservatively keep the quarterly ladder (the proviso's relief covers "an eligible business", and quarterly is the safe reading for mixed income). Verified against FY2026-27 practitioner guidance (ClearTax/Tax2win/India Briefing); no existing profile's figures changed — the only purely-presumptive demo (Grace Thomas) has zero assessed tax after TDS, checked across all 11. **Second refinement, same day (first-class symmetry sweep):** the US twin (Form 2210's $1,000 balance-due de minimis) had been built in from the start, but India's own equivalents were missing — now added: the s.404 (old s.208) ₹10,000 floor (no advance-tax obligation at all below it, so ss.424/425 interest cannot arise), and the s.404 senior carve-out (old s.207(2)): a resident individual aged 60+ with no PGBP-head income is exempt from advance tax entirely — common for retired returnees living on pension/interest/CG; "has PGBP" read broadly (any business entry, F&O, speculative, partner-firm) so the exemption never applies to a marginal business case. Verified with 4 synthetic hand-checks (assessed ₹8,700 → silent; resident senior 68 no-PGBP → silent; identical facts age 45 → fires; senior 68 WITH a 44ADA entry → fires, via the presumptive single-installment branch, proving the two same-day refinements compose) plus the 11-profile regression (all figures unchanged). |
| IN-2 | Late-filing fee + penalty regime (old 234F ₹5,000; 270A under-reporting 50%/200%) | ❌ | 🟡 Needs field | P2 | Natural companion to IN-1. The fee needs an actual-filing-date field Layer 1 doesn't collect; the 270A under-reporting exposure is disclosure-only and could ship without it. |
| IN-3 | **Agricultural income — partial integration** | ❌ | 🟡 Needs field | P2 | Exempt income that still raises the effective slab rate when agri income > ₹5,000 and total income exceeds the exemption limit. No Layer 1 field exists (only a tooltip mention under misc income). Needs one new field + rate-integration math. Very common for HUF/family profiles. |
| IN-4 | **Property sale capital gains — engine-side computation** | 🔍 | 🟢 Now | **P1** | Layer 1 collects the complete property-sale card (acquisition date/cost, improvements, pre-2001 FMV, transfer expenses, stamp-duty-value override, reinvestment-exemption elections, buyer TDS) but the engine appears to consume only pre-aggregated CG figures — the per-property computation (indexation where applicable, 50C deemed consideration, exemption caps: ₹50L bond limit, ₹10cr cap on residential reinvestment) is not verified engine-side. Verify, then close whichever half is missing. |
| IN-5 | AMT for non-corporates (old 115JC, 18.5% on adjusted total income) | ❌ | 🟡 Needs field | P3 | Relevant to firms/LLPs and old-regime individuals claiming specified deductions (10AA/35AD/Ch VI-A part C). Corporate MAT **is** modeled (book-profit proxy); the non-corporate twin needs the specified-deduction detail Layer 1 doesn't break out. |
| IN-6 | Presumptive taxation — s.58 (old 44AD/44ADA/44AE) | 🟡 | 🟢 Now | P2 | Verified current limits: business ₹2cr (₹3cr when cash receipts <5%), professional ₹50L (₹75L when cash <5%), rates 8%/6% and 50% carried into ITA 2025 s.58 unchanged. Layer 1 already has presumptive fields and `updateBizPresumptive`; engine-side depth (limit tests, 5-year lock-in, audit-if-opt-out interplay) is a build against existing data. |
| IN-7 | ~~Tax-audit threshold flag (s.44AB — ₹1cr / ₹10cr when ≥95% digital)~~ **FIXED** | ✅ shipped | — | — | Shipped as part of a Filings-tab coverage pass (13 Jul 2026): new `form_3cb_3cd` document (Form 3CB/3CD, Tax Audit Report), triggered by summing turnover across `business_entries[]` (same gross-receipts/turnover/digital+cash field-fallback chain as `computeBusinessEntryNetProfitInr`) against the ₹1cr/₹10cr(≥95%-digital) threshold, OR unconditionally for a company (`indiaIsCompany` — Companies Act audit is not turnover-gated). The Compliance Calendar's India ITR deadline now also shifts Jul 31 → Oct 31 ("audit case") on the same trigger, instead of always showing the non-audit date regardless of entity type. Verified with synthetic ₹15cr/₹50L/₹105cr turnover fixtures at both sides of the 95%-digital boundary, plus all 9 demo profiles (`tests/engine/run.js`, 20/20 pass). |
| IN-8 | House-property depth: ₹2L loss set-off cap, 30% standard deduction, 1/5 pre-construction amortization | 🔍 | 🟢 Now | P2 | HP income is aggregated and loss set-off exists, but whether the ₹2L inter-head cap and 1/5 amortization are enforced engine-side needs verification, then closing against data already captured. |
| IN-9 | ~~Deduction breadth: 80G, 80E, 80DDB, 80U/80DD (old regime)~~ **MOSTLY FIXED** | 🟡 | ⚪ N/A | P3 | Layer 1 DOES capture all of these (verified 13 Jul 2026 via a field-coverage audit, see `docs/FIELD_COVERAGE_AUDIT.md`) — the engine just never read them. Now wired: 80DD/80U (flat ₹75k/₹1.25L by disability severity, matching Layer 1's own "standard"/"severe" resolution), 80DDB (actual expense capped ₹40k/₹1L by Layer 1's own "normal"/"senior" patient category), 80E (uncapped education-loan interest), 80EEA/80EE (capped by sanction-date window — ₹50k for FY16-17-sanctioned, ₹1.5L for FY19-22-sanctioned, 0 outside both), 80GGB/GGC (uncapped political donations), 80GG (rent-paid-no-HRA, the classic 3-way minimum against gross total income, applied in `computeIndiaTax` where GTI is available). **Still open:** 80G (donations) — per-entry 50%/100% with/without-qualifying-limit categorization needs gross-total-income-dependent capping logic per donation category, deliberately not guessed at; 80M (inter-corporate dividend) — business-entity-only, low relevance for the individual-focused profiles. |
| IN-10 | s.89 salary-arrears relief (Form 10E) | ❌ | 🟡 Needs field | P3 | Needs new fields (arrears by year). Niche but real for job-switchers/PSU arrears. |
| IN-11 | Clubbing depth (s.64 old) | 🟡 | ⚪ N/A | P3 | Raw clubbed amounts are captured and taxed; no detection/validation logic (e.g. spouse-asset-transfer tracing). Accepted as data-entry-driven — recorded decision, not a build item. |
| IN-12 | s.194P (75+ senior, bank files for them — no return needed) | ❌ | 🟢 Now | P3 | Trivial disclosure from DOB + income mix. |
| IN-13 | STT (raised on F&O by Finance Act 2026) | 🚫 | ⚪ N/A | — | Transaction tax, not income tax. `stt_paid` flags already drive the CG regime correctly; the levy itself stays out of scope. |
| IN-14 | GST | 🚫 | ⚪ N/A | — | Indirect tax; out of scope by design. Note only so the decision is recorded. |
| IN-15 | ~~Winnings TDS — s.194B / s.194BA (flat 30%)~~ **FIXED** | ✅ shipped | — | — | User-directed, part of a batch answering "are there similar findings we're missing for the dual-jurisdiction profiles too?" New `winnings_tds_estimate` row on the India Withholding page's estimate section, mirroring the existing `vda_194s_estimate` crypto row exactly: 30% flat on the combined `specialRate115bb` figure (Layer 1's `winnings_lottery_gaming_inr` + `online_gaming_winnings_inr` are combined into one model aggregate with no per-transaction breakdown, so s.194B's ₹10,000-per-transaction floor isn't distinguishable from s.194BA's zero-floor gaming TDS within this annual figure — documented, same class of granularity gap as s.194S's ₹50,000 specified-person threshold). Verified: 27-test suite; hand-checked exact against two real profiles — Rohan Mehta's ₹1,80,000 winnings → ₹54,000 expected TDS, Kavya Iyer's ₹40,000 → ₹12,000, both matching the 30% rate to the rupee. |
| IN-16 | **s.197 Lower-TDS Certificate — engine never consumes it** | ❌ | 🟢 Now | P2 | Layer 1 collects the full certificate (approved rate, validity start/end dates, covered income types: property CG / NRO interest / dividend / royalty-FTS) but nothing engine-side reads it. Should cross-check the certificate rate against actual withholding on covered streams and flag expiry/coverage mismatches. Data fully exists. |
| IN-17 | Form 15G / 15H self-declarations (no-TDS on interest below taxable limit) | ❌ | 🟡 Needs field | P3 | No Layer 1 field. Companion to the in-flight 194A interest-TDS field — a declaration on file explains a legitimately-zero TDS figure. |
| IN-18 | s.206AB higher TDS for ITR non-filers (and s.206AA no-PAN quantification) | 🟡 | 🟡 Needs field | P3 | The PAN-Aadhaar-inoperative banner covers the s.397(2)/206AA-style override qualitatively; the non-filer double-rate rule needs a "filed ITR last year?" field, and neither is quantified per-row. |
| IN-19 | Remaining resident TDS streams: s.194K (MF income), s.194LBA (REIT/InvIT distributions — the `reit_invit` asset class already exists), s.194N (cash withdrawal), s.192 salary-TDS breakout | ❌ | 🟡 Needs field | P3 | Beyond the five streams in the in-flight Antigravity prompt (XB-13). Each needs a Layer 1 field; salary TDS is currently indistinguishable inside the 26AS aggregate. |
| IN-20 | Document-upload extraction is simulated | 🚫 product | ⚪ N/A | — | The "26AS upload" (hardcodes ₹2,84,350), Lower-TDS-cert upload, bank-statement and property-doc uploads are demo mocks, not real OCR/parsing. Recording so nobody mistakes them for live extraction; real parsing is a product build, not a tax-rule gap. |
| IN-21 | ~~CRITICAL — `net_profit_inr` read but never set by Layer 1 India~~ **FIXED** | ✅ shipped | — | — | Real computation added: s.58/44AD (6%/8% digital/cash), s.58/44ADA (50% flat), s.58/44AE (goods-vehicle presumptive from `goods_vehicles[]`, Rs1,000/ton/mo heavy or Rs7,500/mo flat), and regular-books (gross receipts less the unambiguous general-PGBP expense categories — depreciation/F&O/disallowances still Phase 1, see IN-22..25). Hand-injected `net_profit_inr` still honored first (demo profiles), falls through to real computation when absent. Verified: 4 synthetic cases hand-checked exact, 9-profile harness unchanged, Rohan Mehta's consulting entity converted off the phantom field entirely (₹18L x 50% = ₹9L, matching the old injected figure bit-for-bit) and confirmed live in the Business tab. |
| IN-22 | ~~F&O vs speculative income — kept out of ordinary net profit, ring-fenced set-off~~ **FIXED** | ✅ shipped | — | — | F&O (`non_speculative_income_inr`, ordinary PGBP under the s.43(5) proviso) now added directly into business income. Speculative income (`speculative_income_inr`) is genuinely ring-fenced (s.73/s.113) via a new `speculativeInr` bucket in `computeLossSetOff` — a brought-forward speculative loss (`speculativeLossAvailableInr`, modeled since before this session but permanently inert) can now finally net against it; a current-year speculative loss doesn't touch ordinary business income at all. Built correctly rather than copying layer1_india.html's own live preview calculator, which sums speculative income into ordinary business unconditionally (a UI shortcut, not the legally correct treatment). Verified: 25-test suite, 3 synthetic hand-checked cases, live in the Reconciliation tab. |
| IN-23 | ~~Business disallowances not folded into net profit (s.40A(3) cash limits, s.40(a) non-TDS payments, s.43B(h) MSME timing)~~ **FIXED** | ✅ shipped | — | — | Corrects this row's own prior claim: the disallowance fields were NOT already read for a working finding — that claim (and `scope_payer_tds`'s matching one) traced to a dead calculator (`_evaluateSurchargeBuckets` in layer1_india.html) reading a top-level `business_income.expenses` object whose only writers are never called from any rendered input. The real per-unit expense card (`business_entries[idx].expenses`, via `updateUnitExpense`) is what this engine already reads for net profit — s.40(a)(i)/(ia), s.40A(3) (both the ₹10k general and ₹35k transporter cash fields), and s.43B(h) (`msme_payables[]`, unit-tagged like `asset_blocks[]`) now all net into it there. Also fixed in the same pass: s.43B(d) employer PF/ESI was deducted unconditionally since IN-21 shipped — now gated on `employer_pf_esi_paid_before_due_date`. Verified: 25-test suite, 2 synthetic hand-checked cases, live in the Business tab trace. |
| IN-24 | ~~Partner-firm pass-through (`partner_firms[]`) not read anywhere~~ **FIXED** | ✅ shipped | — | — | Remuneration + interest-on-capital (taxable PGBP to the partner) now added into business income; exempt profit share (`profit_share_exempt_inr`, already taxed at the firm level under s.10(2A)) deliberately excluded, not double-counted. The s.40(b) cap on what the firm may pay out is tested at the firm's own return, which this app doesn't prepare, so the entered figure is trusted rather than re-derived. Verified: 25-test suite, 1 synthetic hand-checked case, live in the Reconciliation tab (Rohan Mehta's ₹7,20,000 partner stake). Phase 1 (§2.1-2.4) is now fully shipped. |
| IN-25 | ~~Depreciation (`asset_blocks[]`) not computed~~ **FIXED** | ✅ shipped | — | — | Real s.32 WDV block-of-assets computation (opening WDV + additions − sale, half-rate under the <180-day proviso) plus s.32(1)(iia) additional depreciation for new manufacturing/power-gen P&M (gated on the same regime-disallowance rules Layer 1 itself enforces), matched exactly to Layer 1's own `asset_class` rate table. Nets into business income for regular-books entries only; excess that pushes aggregate business income negative now correctly routes into this year's unabsorbed-depreciation pool (s.32(2)) instead of disappearing. Verified: 25-test suite, 5 synthetic hand-checked cases, live in the Business tab trace. |
| IN-26 | s.44BBB (foreign co. civil construction, 10% presumptive) / s.35AD (100% capex deduction, specified businesses) / s.115V tonnage tax (shipping) — all uncomputed | ❌ | 🟢 Now | P3 | `s44bbb_receipts_inr`, `specified_business_s35AD_inr`, `tonnage_tax_115V_inr` are fully captured (found on re-audit — the first tracker pass truncated its field read and missed these) but never touched engine-side. Smaller affected population than IN-21/22 but genuinely data-complete. See spec §2.7. |
| IN-27 | ~~`commodities.transactions` / `unlisted_equity.transactions` never read anywhere in the engine~~ **FIXED** | ✅ shipped | — | — | Found via a full field-coverage audit (13 Jul 2026, `docs/FIELD_COVERAGE_AUDIT.md`): Layer 1's Commodities module (physical gold/silver, Sovereign Gold Bonds, gold ETF/FoF) and Private-Shares-Transferred module (unlisted company shares) were fully collected and even rendered back onto the form, but zero references existed in `normalize.js`/`computation.js`/`conflicts.js` — any gain here was invisible to tax computation regardless of how carefully it was entered. Now classified and folded into the existing ltcg197/stcgSlab buckets: physical gold/silver/other → general s.112 24mo threshold; SGB (secondary market, or original sold before maturity) → listed 12mo threshold; SGB original redeemed AT maturity → exempt (s.47(viic)); gold ETF/FoF → s.50AA always-short-term (Finance Act 2023); unlisted equity shares → general s.112/s.197 24mo threshold (no STT, so no listed-equity treatment). Single-pass verification only (not the multi-source pass the pre-existing financial_holdings classes received) — re-verify before relying on this for a real filing. |
| IN-28 | ~~8 "Other Sources" fields collected but never read: gifts >₹50k, family pension, spousal clubbing (net of minor-child exemption), non-10(10D) LIC maturity, angel tax, local-authority exemption, misc income, taxable EPF/NPS interest not counted in India's own total~~ **FIXED** | ✅ shipped | — | — | Found the same way as IN-27 — these fed the form's OWN on-page live-preview total (`evaluateSurchargeBuckets`'s `normalSlab`/`otherSourcesAdditions`) but were absent from the engine entirely, so a real filer's amounts here were always ₹0 in the actual computed tax regardless of what the live preview showed on the form. Now summed into a new `otherSourcesMisc` bucket matching the form's own formula exactly (gifts + family-pension-net-of-s.57(iia)-standard-deduction + spousal clubbing − minor-child exemption + LIC maturity + angel tax − local-authority exemption + misc + taxable EPF/NPS interest), flowing into the same slab-rate "Other Normal" pool as interest/dividend. |
| IN-29 | s80G (donations) not read — per-entry 50%/100% with/without-qualifying-limit categorization | ❌ | 🟡 Needs build | P2 | Found during the same audit (IN-27/28) but deliberately not guessed at: each `s80G[]` entry needs its own category (100% no-limit, 50% no-limit, 100% with-qualifying-limit, 50% with-qualifying-limit) and the "with-qualifying-limit" categories cap at 10% of adjusted gross total income — a genuinely gross-income-dependent, per-entry computation, not a flat/simple cap like the 6 sections fixed under IN-9. |
| IN-30 | `nro_repatriation` (cumulative repatriated USD this FY, pending repatriation, TDS-on-NRO-balance flag) never read anywhere in the engine | ❌ | 🟢 Now | P2 | Found during the same audit. Captured by Layer 1 and seeded into demo-profile quarters, but no Limit Monitoring gauge exists for it (unlike FBAR/8938/LRS, which all have one) — the effective annual NRO-to-abroad repatriation ceiling (US$1M/FY under RBI rules, subject to Form 15CA/15CB) is currently untracked. A new gauge, not a tax-computation fix — scoped separately from IN-27/28. |
| IN-31 | **Gift-exemption checkboxes ("received on occasion of marriage" / "received from a specified relative") have no handler at all** | ❌ | 🟡 Needs build | P2 | Found by `scripts/audit/dom-handler-coverage.js`'s first real run (14 Jul 2026), confirmed by direct read (`#os-gifts-marriage`/`#os-gifts-relative`, layer1_india.html ~L3705-3709): no `oninput`/`onchange`, no `addEventListener`, and neither id appears anywhere else in the file. Since IN-28 fixed `gifts_above_50k_inr` to actually reach the engine, this now has real consequence — a preparer has no way to tell the system a gift is s.56(2)(x)-exempt (wedding gift, or from a specified relative) other than the workaround of simply not entering the amount, which loses the amount for recordkeeping too. Needs a field to write to (e.g. `other_sources.gifts_exemption_reason`) plus the exemption logic in `aggregateIndiaIncome`. |
| IN-32 | ~~ITR form determination trusted Layer 1's frontend calculator (or a crude entity-type-only guess) with no independent backend check~~ **FIXED** | ✅ shipped | — | — | User-driven: an independent `computeIndiaItrForm` (computation.js) now mirrors Layer 1's own `evaluateITRForm()` (the AY 2026-27 CBDT-notified 7-form eligibility rules) but runs unconditionally, from model/computed fields already verified against Layer 1's real live-written data — not from the frontend's own precomputed `surcharge_buckets` or persisted `itr_recommendation.form`. Runs for every profile, including hand-authored ones that never touched the browser form (previously left with a hedged "ITR-4 if eligible, else ITR-3" guess). Found and deliberately did NOT inherit three concrete bugs in Layer 1's own calculator: (1) `hasBusiness` reads `business_income.has_business_income`, a "setup wizard" checkbox that desyncs from `has_business_or_fo_income` (the field the real Business/F&O module and everything else — including this engine — actually reads/writes); (2) `hasPartnerIncome` reads `partner_remuneration_inr`/`partner_interest_inr` (singular), fields NO input anywhere in layer1_india.html ever writes — always false; the real data is `partner_firms[]` (Phase 1, §2.3); (3) `isDirector` reads `profile.is_company_director`, a field with no backing UI control at all — always false, documented as a genuine gap rather than a checked "not a director" answer. `buildReturnFormDetermination` (conflicts.js) now surfaces WISING's computation as authoritative and cross-checks it against Layer 1's own value when present, raising a new `india_itr_form_mismatch` finding on disagreement instead of silently picking one. Also fixed in the same pass: the per-entry "if eligible, else" hedge in the Business tab entity list (now states a verified fact about that entry, points to the Return Form card for the checked taxpayer-level answer); a missing closing total row and a vague ROR-vs-entity-type ineligibility message in `businessEntryIncomeTrace`; extended real regular-books business data (exercising depreciation/disallowances) from one demo profile to four (Aarav Sharma, Anita Desai, Rohan Mehta, Sharma HUF — the last one deliberately via an invalid s.44ADA election to exercise the entity-type-exclusion branch, distinct from Rohan's NR-exclusion branch). Caught and fixed a real bug in my own first draft here too: `hasBusiness`/`hasPartnerIncome`/`presumptiveOnly` initially read `model.entity.*` instead of `model.income.india.*`, silently defaulting every profile's business signal to false. Verified: 25-test suite, hand-checked ITR forms across all 9 demo profiles, a synthetic frontend/backend mismatch case, and live in the Filings → Return Form card. |
| IN-33 | ~~MAT book-profit input (`mat_book_profit`, `div-prof-mat-profit`) collected but read by NEITHER Layer 1's own calc NOR this engine — both approximated the s.115JB base as taxable income itself; all 3 company demo profiles used a hand-injected `net_profit_inr` shortcut, bypassing Phase 1 depreciation/disallowances entirely~~ **FIXED** | ✅ shipped | — | — | User-directed, prompted by a direct question: "you see that i have made a very detailed form to collect all the info about the company... you see it right?" Verified against `layer1_india.html`: company entity types use the SAME `business_entries[]`/`expenses`/`asset_blocks[]` infrastructure Phase 1 already built for individuals (companies additionally get `opt_115baa`/`115bab`/`115ba`, `mat_book_profit`, s.79 loss, angel tax, tonnage tax toggles — but the underlying net-profit machinery is identical, including `additionalDepreciationEligibleInr`'s existing company-aware `isConcessionalCompany` gate), so no new mechanism was needed, just real data. Nimbus Analytics (`india_pvt_ltd`), Cloudspire India (`us_ccorp_indian_sub`), and Meridian Holdings (`foreign_holdco_poem_india`) now carry real turnover/expense/`asset_blocks[]` entries engineered to net to the exact same figures the old `net_profit_inr` shortcuts asserted (Rs6cr/Rs8cr/Rs2.2cr respectively) — Cloudspire's figure specifically preserved bit-for-bit since it's precisely mirrored into that same profile's US-side `gilti_income_usd`/`foreign_taxes_usd`. Also fixed: `E.indiaMatBookProfitInr` is now read by `computeIndiaEntityTax`'s MAT floor (`matBaseInr`) instead of a taxable-income proxy — Nimbus's `mat_book_profit` was removed entirely once wiring it in surfaced that Layer 1 nulls the field the instant `opt_115baa` is checked (s.115BAA exempts a company from MAT outright, `div-prof-mat-profit` hidden + cleared) — the old profile carried both simultaneously, a state the real form can never produce. Verified: 25-test suite; all 3 companies hand-checked (base tax, surcharge, MAT-floor comparison, cess) against the new business-entry math, both landing on exact pre-existing totals and on ITR-6. |
| IN-34 | ~~Company tax RATE never distinguished domestic vs. foreign-incorporated — every company, including a POEM-resident FOREIGN company, was taxed under the domestic 22%/25%/30% schedule (7%/12% surcharge) with full access to s.115BAA; s.115BAB (15%, new manufacturing) and s.115BA (25% flat) were read only for the additional-depreciation disallowance check, never for the company's own rate; MAT exemption for a foreign company with no India PE (s.115JB(4A)/(4C)) unmodeled~~ **FIXED** | ✅ shipped | — | — | User-directed, prompted by a direct question ("have we implemented the different types of Tax regimes available for the company based on the residency?"). Place of incorporation — not residency — controls which rate schedule applies: a foreign-incorporated company resident via POEM (worldwide income correctly in scope already) is still categorically ineligible for s.115BA/115BAA/115BAB, domestic-incorporated-only elections. Added the real foreign-company schedule (`INDIA_COMPANY_FOREIGN`: 35% flat — cut from 40% by the Finance Act 2025, this same AY2026-27 — 2%/5% surcharge, both lower than the domestic schedule) and gated it on `residency.india.isIndianCompanyFact === false`; wired s.115BAB/115BA as their own rate branches (`indiaOpt115bab`/`indiaOpt115ba`, previously exposed on the model only for the depreciation check); s.115JB(4A)/(4C) MAT exemption approximated on `model.treaty.hasPE` (no India PE → MAT-exempt, matching the same Phase-0-floor precision convention as the rest of this engine's MAT/AMT handling — the fuller DTAA-residence/no-Companies-Act-registration distinction in the statute isn't modeled). Meridian Holdings (`foreign_holdco_poem_india`, the one demo profile that's genuinely foreign-incorporated) recalculated from ₹61,20,400 (wrong, domestic 25%) to ₹81,68,160 (correct, foreign 35%, MAT-exempt since it has no India PE) — Nimbus Analytics and Cloudspire India (both `is_indian_company: true`) verified unchanged. Verified: 25-test suite, all 3 company profiles hand-checked against the new branching, live in the Business tab regime label ("Foreign Company ITR-6 (35%, MAT-exempt (no India PE))"). |
| IN-35 | ~~Two compounding bugs made the Reconciliation page's "India income — by head" table disagree with the Tax Computation card's income figure~~ **FIXED** | ✅ shipped | — | — | User-directed ("how is the total amount different in both the tables?"), spotted on `india_only_ca_client` (Kavya Iyer): the by-head table totalled ₹1,19,77,000, the Tax Computation card's "Current-year income (before brought-forward loss set-off)" showed ₹1,32,70,000 — a ₹12,93,000 gap. Two distinct, independently-real bugs, both found by hand-tracing against `aggregateIndiaIncome`'s own `total` formula rather than guessing: **(1) Profile data bug** — Kavya's own `ltcg_112a_inr: 210000` was nested inside `domestic_income.capital_gains` alongside `short_term_15_pct`, but `ltcg_112a_inr` is read from a genuinely separate TOP-LEVEL `india.capital_gains` container (`annual.capital_gains`, sourced via `indiaAnnualSlice`'s own top-level read) — NOT the same container despite the identical field name at both levels; nesting it in the wrong place silently dropped her entire ₹2,10,000 of real listed-equity LTCG from the computation. Confirmed against Anita Desai's profile (`india_ror_us_income`), the one pre-existing profile that already uses this field with an explicit code comment proving the split-container pattern was deliberately tested. **(2) UI completeness bug** — the by-head table itself (`ReconciliationView`, Views.jsx) only ever rendered 9 of the 14 income categories `aggregateIndiaIncome`'s `total` formula actually sums; missing rows: `otherSourcesMisc` (₹3,13,000 for Kavya), `ltcg197Inr` (s.197 LTCG, no exemption — ₹7,40,000), `vdaGainInr` (s.115BBH crypto — ₹2,40,000), plus `stcgSlabInr` and `chapterXiiaInvestmentIncomeInr` (both ₹0 for Kavya but real for other profiles) — the total these fed WAS always correct end-to-end in the actual tax computation (Reconciliation's income figures were never wrong for tax purposes), only the disclosure table summing them for the professional's own sanity-check was incomplete. Fixed both; also renamed "Long-term capital gains" → "Long-term capital gains (s.112A, listed equity)" and "Short-term capital gains" → "Short-term capital gains (s.111A, 20%)", adding a new "Short-term capital gains (unlisted, slab rate)" row, now that multiple STCG/LTCG-type rows sit side by side and need to stay visually distinguishable. The remaining, EXPECTED gap between the now-complete by-head total (₹1,34,80,000) and the Tax Computation card's figures is legitimate tax mechanics, not a bug, and is already self-documented by the card's own existing click-to-expand trace text: ₹1,25,000 s.198/112A LTCG exemption (₹1,34,80,000 → ₹1,33,55,000), a ₹90,000 brought-forward STCG-loss set-off (→ ₹1,32,65,000 "Gross total income"), and ₹7,08,000 of Chapter VI-A deductions (→ ₹1,25,57,000 "Total income") — each already an itemized line in the Tax Computation card's own waterfall. Verified: 27-test suite; hand-recomputed exact (both the ₹13,480,000 by-head total and the full deduction/exemption/set-off waterfall down to ₹12,557,000) via a debug script reading the live model/computed objects directly; live screenshot of Kavya's Reconciliation page confirms all rows now present and the total ties out. |

| IN-36 | **Old-vs-new regime comparison — the engine computes ONLY the regime already chosen on Layer 1 (`model.residency.india.taxRegime`, default NEW); no side-by-side, no "you chose the costlier regime" finding** | ❌ | 🟢 Now | **P1** | Found during the 16 Jul 2026 "what does a tax pro actually buy" audit. "Which regime should my client pick" is the single most common question an Indian CA answers for every individual client every year, and every mainstream India prep suite ships a comparator — its absence is conspicuous in a CA-facing demo. All inputs already exist: `computeIndiaTax` is already fully parameterized by regime (slabs, §156/87A caps, and the deduction set all branch on `isNew`), so running it twice and surfacing the delta plus a `regime_suboptimal` finding when the chosen regime is costlier is pure engine/UI work, zero new fields. One real subtlety to model honestly: s.115BAC(6) — a taxpayer WITH business/professional income can switch out of the new regime only once in a lifetime (Form 10-IEA) while the salaried can flip annually, so the finding must frame "old is cheaper this year" much more cautiously for business filers (this year's saving can cost more later). |
| IN-37 | ~~7 individual/HUF residency facts (`days_in_india_preceding_4_years_gte_365`, `employment_or_crew_status`, `came_on_visit_to_india_pio_citizen`, `nr_years_last_10_gte_9`, `days_in_india_last_7_years_lte_729`, `india_source_income_above_15l`, `liable_to_tax_in_another_country_being_indian_citizen`) collected by Layer 1's residency wizard, read NOWHERE in the engine~~ **FIXED** | ✅ shipped | — | — | Found during a `prototypes/graph-pilot` DAG-migration session (19 Jul 2026) auditing whether the DAG's own residency consistency check had "considered everything Layer 1 India collects for residency" — it hadn't, and neither had the production engine. `layer1_india.html`'s `runResidencySolver()` is a full ~20-branch s.6/s.6(1A) determination (60/182-day tests, 4-year lookback, RNOR sub-status via the 9-of-10-years and 729-day-in-7-years conditions, employment/crew departure exception, PIO/citizen visit exception, deemed-residency for high-income citizens not liable to tax elsewhere) that only ever crossed into `normalize.js` as its own already-decided conclusion (`final_india_residency_status`) — same "collected but never read" class as IN-27/28, just never previously audited for this specific area. Now read into `model.residency.india` alongside the existing `status`/`daysCurrentYear`/`isIndianCompanyFact`/`indiaWhollyOutsideIndiaFact` fields, plus the new `dtaa_worldwide_ceded` fact (see the companion Layer 1 fix, same session, that stopped `runResidencySolver()` conflating the DTAA Article 4 tie-break with domestic residential status). Purely additive — no computation changed, nothing yet CONSUMES these fields (that's IN-38 for the one concrete consumer already identified, and the natural next step for a real engine-side residency-consistency finding mirroring the one already built and verified in the DAG). Verified: 27-test suite unchanged; all 11 real profiles read back the new fields without error (all null/false, matching that the demo fixtures never populate this fact set — confirmed, not assumed, during the same session's DAG work). |
| IN-38 | **`aggregateIndiaIncome`'s `isIndiaRor` gate (the sole mechanism that excludes foreign `financial_holdings` transactions from India's worldwide tax base) checks only `final_india_residency_status === "ROR"` — never the DTAA treaty-cede fact, so a domestically-ROR taxpayer who ties the Article 4 test to the US has their foreign income wrongly included** | ❌ | 🟢 Now | **P1** | Found immediately after IN-37/the Layer 1 DTAA-conflation fix, same session, while double-checking a claim made mid-session that treaty-cede was "already handled correctly, separately, by `residency.india.worldwide`" — that claim was wrong and corrected on the spot rather than left standing. Traced the actual consumers of `residency.india.worldwide` (`computeFtc`'s credit-relief math, `crossBasis`'s planning-grade display table, `monitoring.js` badges) and confirmed NONE of them feed back into what `computeIndiaTax`/`aggregateIndiaIncome` actually tax — `computeIndiaTax` doesn't even receive the treaty-aware `residency` object as a parameter, only `model.residency.india.status`. Before the Layer 1 fix (IN-37's companion), this was masked by an unrelated bug: `final_india_residency_status` used to collapse to `"NR"` on a US tie-break, which accidentally also zeroed `isIndiaRor` and excluded the foreign income — right dollar answer, wrong mechanism, and it broke ~20 other domestic-law-only checks in `layer1_india.html` in the process (s.44BB eligibility, s.87A rebate, domestic-company classification) that all expected pure, treaty-independent domestic status. Now that the conflation is fixed, this accidental exclusion no longer happens, so this row is a real, live regression, not a pre-existing one. Fix is small and the field already exists (IN-37): `isIndiaRor` should read `status === "ROR" && !model.residency.india.dtaaWorldwideCeded`. Not yet built — flagged, not fixed, pending confirmation this scope (an actual tax-base change, not just a new field read) should proceed. |

**Buildable-now count (India): 9 of 23 remaining** (IN-1, IN-7, IN-9 partial, IN-15, IN-21, IN-22, IN-23, IN-24, IN-25, IN-27, IN-28, IN-32, IN-33, IN-34, IN-35, IN-37 shipped — Phase 1, §2.1-2.4, fully complete, plus the ITR solver, company/MAT wiring, the domestic/foreign company rate split, advance-tax interest, winnings TDS, the income-by-head reconciliation fix, and the individual/HUF residency field reads) — IN-4, IN-6, IN-8, IN-12, IN-16, IN-26, IN-30, IN-36, IN-38.

**See also `docs/BUSINESS_ENTITY_ARCHITECTURE.md`** for the full multi-entity architecture spec (entity graph, inter-entity flows, phased build order) that IN-21 through IN-25 and US-16/17 all fold into — that document is the source of truth for sequencing this work; don't duplicate the phase plan here.

**Verified current (India, 11 Jul 2026):** Finance Act 2026 (assented 30 Mar 2026) made **no slab changes** for TY2026-27 — new-regime slabs and the ₹60,000 / ₹12L §87A-equivalent rebate stand as already built; buyback-as-capital-gains from 1 Apr 2026 is already modeled; ITA 2025 renumbering already applied throughout.

**Filings-tab coverage pass (13 Jul 2026):** the Documents-to-File catalogue and Compliance Calendar were audited against "every relevant form for a real Wising user," not just the demo profiles. Shipped from that audit, beyond IN-7 above: `schedule_al` (Schedule AL, Assets & Liabilities — ₹50L total-income threshold, ITR-2/3 only, excluded for company/firm which carry their own unconditional balance-sheet requirement) and `form_8802` (US Form 8802, the prerequisite IRS application for the Form 6166 that India's existing `trc` document expects — same trigger condition as `trc`, framed for the US-side filer). Each deadline in the Compliance Calendar now also carries a precise `docIds` list (which of these are actually filed with it) instead of a blanket per-jurisdiction guess, and the calendar's US/India filing dates vary by the already-determined return form (1120/1120-S/1065/1041/ITR-6 each get their real statutory dates) instead of always showing the individual 1040/ITR-2,3 dates. Remaining known gaps in this same sweep (706-NA/709 estate exposure, Form 8854, 8858/926, ITR-U, Form 10E, 15G/15H, US state returns) are tracked at XB-1, XB-5, XB-6 and are 🟡/🚫 — see those rows for why.

---

## B. US-only lens (domestic taxpayer, no India exposure)

| ID | Item | Status | Build Now? | Priority | Detail |
|---|---|---|---|---|---|
| US-1 | ~~Estimated-tax underpayment penalty (§6654 / Form 2210)~~ **FIXED** | ✅ shipped | — | — | A partial version of this finding already existed (90%-of-current-year safe harbor + raw balance-due as a stand-in amount, explicitly self-documented as not checking the prior-year harbor) — found while building IN-1's mirror. Real gap: Layer 1 US's `#with-prior-tax` input (`withholding_and_estimated.prior_year_total_tax_usd`) was collected but never read anywhere in the engine, so the 100%/110%-of-prior-year safe harbor could never actually be checked, only asserted as "not checked" in the finding's own text. Now wired (`null`, not 0, when never entered, so "no prior-year tax" isn't confused with "not answered"); the 110% multiplier (vs 100%) applies when current-year AGI exceeds $150k, used as a practical proxy for prior-year AGI since Layer 1 has no such field. Also upgraded from showing the raw balance-due figure to an actual estimated PENALTY dollar amount: simplified Form 2210 Part III regular method — four equal required installments, withholding spread evenly across all four quarters (the real IRS convention), estimated payments applied to their own quarter, each quarter's shortfall accruing independently (no cross-quarter netting) at that quarter's verified TY2026 IRS underpayment rate (7%/6%/7% Q1-Q3; Q4 approximated at Q3's rate, documented as unverified) for the months remaining to the following 15 Apr. Verified: 27-test suite; confirmed it correctly does NOT fire for `us_only_cpa_client` (safe-harbored — prepaid $73,000 against a $49,492 liability) while firing with real, sane, previously-invisible dollar amounts across 4 of the 9 pre-existing dual-jurisdiction profiles. |
| US-2 | ~~Social Security benefit taxability (0/50/85% provisional-income tiers)~~ **FIXED** | ✅ shipped | — | — | `aggregateUsIncome` used to fold 100% of `social_security_benefits_usd` into taxable retirement income, overstating tax for every SS-receiving profile. Now correctly limited to 0%/50%/85% via `computeSsTaxableUsd()` in computation.js — a line-for-line transcription of IRS Pub 915 Worksheet 1 (base/additional thresholds added to `constants.js`, NOT inflation-indexed, fixed by statute since 1984/1993; MFJ $32k/$44k, single/HoH $25k/$34k, MFS conservatively defaulted to the punitive $0/$0 "lived with spouse" treatment since Layer 1 US doesn't capture the "lived apart all year" exception fact). Preferential income and tax-exempt interest (`interest_us_exempt_usd`, itself previously unread — see IN/US-audit history) both correctly count toward the "provisional income" test. Verified against 4 independently hand-computed worksheet cases (0%, tier-2, tier-3-capped-at-85%, and MFJ) before being locked into `tests/engine/fixtures.js`/`run.js`. Made demo-visible: Grace Thomas (`us_citizen_expat_india`, age 67) now carries `social_security_benefits_usd: 24000` — her provisional income lands just above the single-filer base threshold, so ~11% of her benefit is taxable (checked via `socialSecurityDetail` on her computed profile, not just the isolated fixture cases). A new `socialSecurityDetail` trace object exposes the full worksheet breakdown for UI transparency, matching the existing `seniorDetail`/`ctcDetail` convention. |
| US-3 | Capital-loss $3,000/yr limit + carryover; wash sales | 🔍/❌ | 🟢 Now/partial | P2 | The current-year $3,000 limit against ordinary income is computable now from existing gain/loss totals. Carryover to future years needs a new "prior-year capital loss carryover" field; wash-sale detection is unmodeled at disclosure level even with new fields. |
| US-4 | §121 home-sale exclusion ($250k/$500k MFJ) | ❌ | 🟡 Needs field | P2 | `real_estate.properties` captured, but no sale price / ownership-and-use-test dates. Common and high-dollar. |
| US-5 | ~~Retirement mechanics: §72(t) 10% early-withdrawal penalty~~ **FIXED (early-withdrawal penalty only)** | ✅ shipped (partial) | — | — | Computed from `ira_distributions_usd` + `401k_distributions_usd` (deliberately excluding pension/annuity income and Social Security, which have their own/no early-distribution concept — a new `retirementDistributionsSubjectTo72tUsd` field isolates just the two) + DOB (age <59 at year-end test): flat 10% additional tax on the full distribution whenever the age test fails. Known simplification, explicitly flagged in the finding itself: Layer 1 has no field for any real statutory exception (death, disability, SEPP, first-time-homebuyer $10k, higher education, medical-over-7.5%-AGI, qualified birth/adoption) — assumes the full 10% until a preparer confirms otherwise, which can overstate the real number for a taxpayer who genuinely qualifies for an exception. **RMDs (age 73; 75 from 2033) and the excess-contribution excise remain unbuilt** — both need account *balances*, which still aren't captured anywhere in Layer 1 US; IN-1 stays "partial" for exactly this reason. Verified: 27-test suite; hand-checked exact ($2,000 = 10% × $20,000) against `us_only_cpa_client` (age 47, took $12,000 IRA + $8,000 401(k) distributions the same year as active 401(k)/HSA contributions — a realistic "old employer's account partially cashed out while still saving elsewhere" scenario). |
| US-6 | §3406 backup withholding, §3405 pension withholding, W-2G gambling withholding | ❌ | 🟡 Needs field | P2 | Carried from the withholding work: Layer 1 has gross-only fields for 1099/retirement/gambling income — no withholding sub-fields. Needs Layer 1 additions (previously offered as option 4). |
| US-7 | Form 1099-DA digital-asset broker reporting | ❌ | 🟢 Now | P3 | Verified: gross-proceeds reporting mandatory for 2025 transactions (filed early 2026); **basis reporting mandatory for covered assets from 1 Jan 2026**. `has_crypto` already exists; add an awareness/reconciliation finding. |
| US-8 | FEIE housing exclusion/deduction | ❌ | 🟡 Needs field | P3 | FEIE itself is modeled; the §911(c) housing component needs housing-expense amounts Layer 1 doesn't collect. |
| US-9 | Form 1116 FTC baskets (passive vs general), HTKO | 🟡 | 🟢 Now | P2 | Single-limitation FTC is modeled; the income streams needed to split into passive vs general baskets (interest/dividends vs wages) are already categorized in the data — this is a computation-depth build, not a missing-field one. |
| US-10 | Kiddie tax (§1(g)) | ❌ | 🟡 Needs field | P3 | Dependent count exists; unearned-income-by-child detail doesn't. Low priority for the audience. |
| US-11 | EITC / ACA premium tax credit / Saver's credit | ❌ | 🟡 Needs field | P3 | Low-income-skewed credits; audience skews high-income. Needs credit-specific inputs Layer 1 doesn't collect. Likely 🚫 by design — decide and record. |
| US-12 | State income tax **computation** | ❌ | 🟢 Now (large) | P2-decide | Residency conflicts (CA/NY rules) are modeled but computed state tax is $0 everywhere. Residency + income data already exists — this is a large engine build, not a blocked-on-fields one. Either build top-5 NRI states (CA/NY/NJ/TX/WA — two of which have no income tax) or explicitly banner "federal only." |
| US-13 | **FICA withholding invisible on the Withholding page** | ❌ | 🟢 Now | P2 | Layer 1 US already captures `ss_tax_withheld_usd` (box 4) and `medicare_tax_withheld_usd` (box 6) per W-2, but the Withholding page shows only federal + state. Pure display addition — data fully exists, no new fields. |
| US-14 | **Excess Social Security withholding credit (multiple employers)** | ❌ | 🟢 Now | P2 | When two employers each withhold 6.2% up to the wage base, the combined excess over one wage-base-worth is a refundable credit (Schedule 3). Computable from the same per-W2 box-4 fields Layer 1 already captures. Wage base: $176,100 for 2025; **2026 figure must be verified before building** (SSA COLA announcement). Directly relevant to job-switcher profiles like Aarav Sharma. |
| US-15 | FICA/FUTA as a levy (employer + employee employment tax) | 🚫 recorded | ⚪ N/A | — | Different tax base from income tax; only Additional Medicare 0.9% (modeled ✓) and the two withholding-visibility items above intersect this app. Recording the boundary so it isn't re-litigated. |
| US-16 | ~~`guaranteed_payments_usd` dropped from partnership K-1 income entirely~~ **FIXED** | ✅ shipped | — | — | Bigger than scoped: Box 14A (`self_employment_earnings_usd`, the K-1's own authoritative, partner-type-aware SE-tax figure) was never read at all — partnership SE tax was unconditionally $0 for every K-1. Guaranteed payments now flow into `businessUs`; Box 14A now drives SE tax (falls back to guaranteed + ordinary-if-general when absent); QBI correctly stays ordinary-income-only. A self-introduced double-count bug (QBI briefly inheriting Box 14A via `qbiIncome = seEarnings`) was caught by hand-verification before shipping and fixed by reordering. Verified: Rohan Mehta gained a general-partner K-1 ($18k ordinary + $12k guaranteed, Box 14A $30k) — businessUs +$30k (was +$18k pre-fix), SE tax reflects the full $30k, QBI deduction exactly $16,000 = 20% x $80,000. |
| US-17 | ~~`trusts_estates_k1[]` entirely absent from `businessEntities()`~~ **FIXED** | ✅ shipped | — | — | See US-28 — fixed in the same pass as the ccorp/scorp/partnership/trust form audit. |
| US-18 | US depreciation / §179 / bonus depreciation — full MACRS asset system uncomputed | 🟡 partial | 🟢 Now | P2 | Found on re-audit (missed in the first pass, which only checked India-side depreciation): the asset-row UI shared across Sch C/farm/rental/K-1/1120 already carries real MACRS class rates, §179 amount (with the form's own published caps), bonus-depreciation election, cost basis, and placed-in-service date. **Now substantially closed for Schedule C** (self-employment) — see US-29. Previously closed by US-28: the K-1-level §179 deduction box and C-corp/S-corp/partnership Schedule M-1 book-to-tax reconciliation. Still open: the same per-asset computation for K-1/1120 asset rows themselves (semantically murky — the entity, not the K-1 recipient, already computes and reports its own depreciation before flow-through) and `farming_schedule_f[]` (its net profit is always a flat, already-net manual figure with no real income-statement derivation to net a new deduction into, unlike self-employment's own `computeSelfEmploymentNetProfitUsd`). See spec §2.6. |
| US-19 | ~~Child & Dependent Care Credit read a field name (`dependent_care_expenses_usd`) that exists nowhere in Layer 1 US~~ **FIXED** | ✅ shipped | — | — | Found via the same 13 Jul 2026 field-coverage audit as IN-27/28 (`docs/FIELD_COVERAGE_AUDIT.md`). The real field Layer 1's `#ded-care` input writes is `child_and_dependent_care_expenses_usd` — the credit computed to $0 for every filer no matter what was entered. Fixed to read the real field name. |
| US-20 | ~~SE health-insurance (§162(l)) and SE retirement-plan (SEP-IRA/Solo 401k, §404) above-the-line deductions collected but never read~~ **FIXED** | ✅ shipped | — | — | Layer 1's `#se-ded-health`/`#se-ded-ret` inputs already persisted correctly; the engine just never read `se_health_insurance_deduction_usd`/`se_retirement_deduction_usd` back out, so AGI was always overstated by the full amount of both for a self-employed filer. Now applied above-the-line alongside student-loan interest and half-SE-tax; SE health insurance floored at net SE income (a Phase-0 floor, not the exact §162(l)(2)(A) earned-income limitation). |
| US-21 | ~~AMT private-activity-bond interest read wrong field paths (`itemized_deductions_and_credits.*` / `amt.*`, neither of which exist) instead of the real `amt_inputs.private_activity_bond_interest_usd`~~ **FIXED** | ✅ shipped | — | — | Same audit. The old paths are kept as harmless no-op fallbacks for any hand-authored profile that used one of those shapes. |
| US-22 | **"Passive & Other Income" screen — most inputs have no `oninput`/`onchange` handler at all** | ❌ | 🟡 Needs build | **P1** | Found via the same audit: `layer1_us.html`'s entire Schedule B/E/1099-G "Passive & Other Income" screen (screen 3C-3) was built visually but never wired to `usState` — typing into most of these fields did nothing. **Fixed this session** (now wired to the existing `interest_us_source_usd`/`ordinary_dividends_us_source_usd`/`qualified_dividends_us_source_usd`/`rental_income_us_source_usd` fields the engine already reads): US bank/treasury/OID/seller-financed interest (summed into one taxable total; tax-exempt interest kept separate and correctly excluded), ordinary dividends, qualified dividends, rental income. **Still entirely unwired** (no `id`, no handler, would need new schema fields + engine treatment, not just a wiring fix): state/local tax refunds (1099-G), unemployment compensation, Social Security benefits (this screen's copy — a working `social_security_benefits_usd` field already exists and IS read, reachable elsewhere in the form; this specific input is an orphaned duplicate), alimony received, royalties (Schedule E), cancellation of debt (1099-C), HSA/MSA distributions (1099-SA), and misc/gambling income. |
| US-23 | **No manual-entry UI exists at all for US STCG/LTCG** (`stcg_us_source_usd`/`ltcg_us_source_usd`) | ❌ | 🟡 Needs build | **P1** | Found during US-22's investigation: the engine reads these two fields, and restoration code references `#inc-us-stcg`/`#inc-us-ltcg` elements — but neither element exists anywhere in the DOM. For a real user (not a hand-authored demo profile), there is currently no way to enter US capital gains at all. Needs new input elements + wiring, analogous to the US-22 fix. |
| US-24 | **No manual-entry UI exists at all for the entire US retirement-income group** (`ira_distributions_usd`, `401k_distributions_usd`, `social_security_benefits_usd`, `pension_income_usd`) | ❌ | 🟡 Needs build | **P1** | Found by `scripts/audit/field-coverage.js`'s first real run (14 Jul 2026). All four are read by `aggregateUsIncome`'s `usRetirementIncome` sum but have zero occurrences anywhere in `layer1_us.html` beyond their own schema declaration (`: null,`) — confirmed by direct grep, not just the audit script. The one "Social Security Benefits (SSA-1099)" label that exists on the Passive & Other Income screen has no `id` at all, so it isn't even reachable by the restoration code, let alone writable — same screen as US-22, but this specific field wasn't wired even when the sibling interest/dividend/rental fields were fixed there. A real US-resident or dual-status filer with any IRA/401(k)/pension/Social Security income currently has no way to enter it. Needs new input elements + wiring, same shape as the US-22/US-23 fixes. |
| US-25 | **Form 5472 "Total Intercompany Payments to Foreign Owner" field has no handler, and no engine-side field exists to receive it either** | ❌ | 🟡 Needs build | P2 | Found by `scripts/audit/dom-handler-coverage.js`'s first real run (14 Jul 2026), confirmed by direct read (`#f5472-payments`, layer1_us.html ~L3176): no `oninput`, no id-based `addEventListener` anywhere. Sits inside the gated "Form 5472 (Inbound)" disclosure card (foreign-owned US corp intercompany-payment reporting, `corporate_international.form_5472_related_parties[]` is the sibling array field and IS otherwise present in the schema) but this specific scalar amount was never wired on either end — no state field name was ever decided for it, and `normalize.js` has no Form 5472 read at all. Needs a field name decided, DOM wiring, and an engine read/disclosure — a full build, not a one-line fix. |
| US-26 | ~~The real "Add Foreign Corporation" UI is completely disconnected from the engine — ZERO field names match~~ **FIXED (ownership/identity)** | ✅ shipped (partial) | — | — | Found by `scripts/audit/array-item-coverage.js`'s first real run (14 Jul 2026). `syncCorpState()`'s real field names (`corporation_name`/`country_of_incorporation`/`ownership_percentage`) are now aliased in `normalize.js`'s `businessEntities()` alongside the short names the India→US auto-hydration shortcut uses (`corp_name`/`country`/`ownership_pct`) — CFC ownership % and entity name/country now read correctly for a manually-entered foreign corporation regardless of which of the two construction paths populated it. Verified both shapes independently (no regression to the auto-hydration path) and locked in with a fixture test. Made demo-visible: Vikram Rao (`founder_indian_company`)'s `foreign_corporations` entry was converted from the old short field names to the real `corporation_name`/`country_of_incorporation`/`ownership_percentage` shape, so his demo now exercises the alias fix instead of bypassing it. **Still $0 for a manual entry**: `gilti_income_usd` — the manual card has no GILTI input at all, and adding one requires deciding what a preparer could plausibly enter (a hand estimate, same as the auto-hydration shortcut already allows) vs. waiting for the real quantification build. Deliberately left alone as the separate, larger **XB-14** gap (needs actual CFC financials — E&P, QBAI, tested income — not a rename) rather than guessed at here. |
| US-27 | ~~FEIE only ever excluded `foreign_wages` — self-employment income had no foreign-source path at all, so a real foreign consultant's FEIE-eligible earnings were fully taxed with zero exclusion; the SSTB checkbox (`is_specified_service_trade`, the real persisted field name on every entity type's Sch-C/K-1 row) was read under the wrong names (`is_sstb`/`sstb`), so it never phased out or eliminated QBI regardless of what a preparer checked; the cross-basis "Business/Professional income" reconciliation row hardcoded the US side to $0 unconditionally~~ **FIXED** | ✅ shipped | — | — | User-directed, surfaced while recharacterizing Grace Thomas (`us_citizen_expat_india`, see IN-32's profile list) from India-side salary to genuine s.44ADA business income — her US side needed to correspondingly reflect real self-employment, not wages, and doing that correctly exposed all three gaps above. A Schedule C row flagged `llc_type: "foreign_disregarded"` (Layer 1 US's own Foreign-Disregarded-Entity flow, `addSeBusinessRow`/Form 8858) is the one real signal that a self-employment business is foreign-earned — now carved out of `businessUs` into a new `foreignSelfEmployment` bucket (mirroring how `foreignWages` is already split from `wages`), FEIE-eligible (combined against one shared cap with foreign wages, matching Layer 1's own single `feie_amount_claimed_usd` field, which likewise never distinguished the underlying source array) but still fully subject to SE tax (§1401 is never reached by the §911 exclusion) and still counted toward QBI and the ACTC earned-income floor. `is_specified_service_trade` (verified as the real field across `self_employment`/`s_corporations_k1`/`partnerships_k1` via `.se-sstb`/`.scorp-sstb`/`.part-sstb`) now actually drives the SSTB QBI phase-out. The "Business/Professional income" cross-basis row now compares against `foreignSelfEmployment` instead of a hardcoded zero. Verified: 25-test suite; Grace Thomas hand-checked — $60,241 foreign self-employment earnings, FEIE excludes the full amount from income tax (`incomeTaxUsd` $0), but Schedule SE now correctly charges $8,512 SE tax she'd never have owed under the old wages characterization. |
| US-28 | ~~Audit of `tax_entity_type` ccorp/scorp/partnership/trust found: (1) **critical** — S-corp K-1 Box 1 ordinary income (`ordinary_income_usd`, the real field `syncScorpK1State` writes) was read under field names (`scorp_income_usd`/`ordinary_business_income_usd`) that exist NOWHERE in layer1_us.html, so it silently computed to $0 for every S-corp K-1 ever entered, regardless of amount; (2) `business_income_usd` — the field a ccorp/scorp/partnership-type FILER's own top-line income was supposed to flow through — has zero real UI writing it anywhere on the live form (confirmed by direct grep), so an entity-type filer's own return always computed $0 unless a hand-authored demo profile injected the field directly; (3) `corporate_financials.schedule_m1`/`schedule_l`/`schedule_m2` (the real Schedule M-1 book-to-tax reconciliation Layer 1 US collects specifically for this case) was read by NEITHER Layer 1's own calc NOR this engine; (4) every K-1 type's interest/dividend/capital-gain/rental/royalty boxes (real money, not headline ordinary income) were completely unread, for ALL THREE K-1 types, not just trusts (US-17); (5) `trusts_estates_k1[]` was 100% unread, confirming US-17; (6) the Business tab's own display code (`businessEntities()`) carried a SEPARATE, independently-buggy copy of the same S-corp field-name bug and the same wrong K-1 name-field fallback (`corp_name`/`partnership_name` instead of the real `business_name`)~~ **FIXED** | ✅ shipped | — | — | User-directed full audit of "each tax entity type having their own specific form... please follow that properly," extending the same rigor just applied to India's company forms to the US side. Fixed: S-corp Box 1 now reads `ordinary_income_usd` first (old names kept as harmless fallbacks); `computeUsEntityTax` and `businessEntities()`'s own self-entity row now derive the entity's own taxable income from Schedule M-1 (`net_income_per_books` + `federal_tax_expense` + `meals_disallowed_50` + `foreign_taxes_credited` + `interest_expense_limitation` + `other_additions` − `tax_exempt_interest` − `tax_depreciation_over_book` − `other_subtractions`, the real Form 1120 Line 1-10 structure) whenever Layer 1 US actually collected one, falling back to `business_income_usd` only when it didn't (trusts excluded — Form 1041 doesn't carry Schedule M-1, matching Layer 1's own `isCorpOrPartnership` gate); a new `k1PassiveIncomeUsd()` helper folds interest/ordinary+qualified dividends/STCG/LTCG(+net s.1231 gain)/rental+royalty boxes from all three K-1 types into the same aggregate buckets their directly-held counterparts already use, bridging the two real field-name variants (`royalties_usd` vs `royalty_income_usd`; `net_sec1231_gain_usd` vs `sec1231_gain_usd`); `trusts_estates_k1[]` now feeds ordinary income into `businessUs`/QBI and gained a `businessEntities()` display row (closes US-17); the K-1-level s.179 deduction box now reduces the ordinary income/QBI figure for both partnership and S-corp K-1s; the SSTB fix from US-27 extended to cover `trusts_estates_k1` too. `businessEntities()`'s own separate copies of both bugs (S-corp field name, K-1 name-field fallback) fixed to match. Extended to real profile data, not just a synthetic fixture: Rohan Mehta (`us_resident_indian_income`) gained a real S-corp K-1 (proving the Box 1 fix with a nonzero live figure) and a passive-income/s.179 extension to his existing partnership K-1; Grace Thomas (`us_citizen_expat_india`) gained a family-trust K-1 tied to her existing covered-expatriate-father narrative (proving the trust fix); Cloudspire Inc (`us_ccorp_indian_sub`) converted from the dead `business_income_usd` shortcut to real Schedule M-1 data netting to the exact same $4,260,000 the shortcut asserted. Verified: 25-test suite, a synthetic fixture exercising all three K-1 types simultaneously (hand-checked every aggregate bucket), and both extended demo profiles hand-checked against the new business-entity display. |
| US-29 | ~~Real per-asset MACRS/§179/100%-bonus depreciation for Schedule C (self_employment[].assets[]) entirely uncomputed — Layer 1 US's own local preview calculators (3 near-identical hand-rolled copies) treat every asset as brand-new every year regardless of placed_in_service_date (no year-2+ table exists in its code at all), and hardcode stale figures throughout: 20% bonus depreciation (labeled in its own tooltip as "the 2026 rate" — actually pre-OBBBA law; OBBBA permanently restored 100% for property placed in service after 19 Jan 2025) and a $1.2M/$3M §179 cap/phase-out (2024 figures, inconsistent with THREE different stale numbers scattered across Layer 1's own UI copy — $1.16M/$2.89M in one tooltip, $1.22M in a help card, $1.2M/$3M in the actual code — none matching current law or each other)~~ **FIXED** | ✅ shipped | — | — | User-directed ("go after MACRS depreciation... it's the largest of what's left"). Verified the real 7-field asset-row schema (`id/name/class/cost/sec179/bonus/placed_in_service_date`) is byte-identical across all 6 business array types (self-employment/farm/partnership/S-corp/trust/C-corp K-1s and 1120), confirmed `real_estate.properties[]` has NO depreciation fields at all (a completely different, capital-gains-on-sale-only component) so no rental-property depreciation exists to build. Scoped to `self_employment[]` only — K-1/1120 asset rows and `farming_schedule_f[]` deliberately left unwired (see US-18's remaining-gap note) rather than guessed at. Implemented real IRS Pub 946 Table A-1/A-2 multi-year MACRS tables (200%/150% DB, half-year convention, verified against Layer 1's own year-1 rates matching exactly) plus mid-month-convention straight-line for real property/amortization classes — genuinely tracks which "year N" of an asset's recovery period applies via `placed_in_service_date`, unlike Layer 1's own first-year-only preview. §179: real TY2026 OBBBA figures ($2,560,000 cap / $4,090,000 phase-out threshold, verified via web search), aggregated and phase-out-tested across every asset for the WHOLE taxpayer (not per-asset), with a business-income limitation (excess simply denied, not carried forward — no multi-year state exists in this engine) and proportional scaling across businesses/assets when elections exceed the cap. Bonus depreciation: 100% (OBBBA-permanent), not Layer 1's stale 20%. Wired into `computeSelfEmploymentNetProfitUsd` (only when the entry uses the derived gross-receipts-less-expenses path, never for a `self_employment_earnings_usd`/`net_profit_usd` override — an already-final black-box figure this engine can't know whether depreciation was already reflected in) and into the Business tab's per-asset trace. Made demo-visible: Grace Thomas (`us_citizen_expat_india`) gained a laptop asset (100% bonus depreciation, fully expensing the $2,500 cost in year 1) — net self-employment earnings $60,241 → $57,741, correctly flowing through to a lower SE tax ($8,512 → $8,159) and FEIE exclusion. Documented, not modeled: real property used predominantly outside the US (s.168(g)) must use the Alternative Depreciation System (straight-line, longer recovery, no bonus) — Layer 1's own asset-row form has no field to signal this, so it isn't guessed at even for Grace's own foreign-use laptop. Verified: 25-test suite, 3 synthetic fixtures (multi-asset current+prior-year mix hand-checked exactly, cross-business §179 cap scaling hand-checked exactly, phase-out threshold hand-checked exactly), live in the Business tab trace. |

| US-30 | **FTC carryovers exist only as an OUTPUT — no prior-year carryover input field, no per-basket §904 limitation, no 1-back/10-forward expiry tracking (Form 1116 Schedule B)** | ❌ | 🟢 Now (current-year basket split) / 🟡 Needs field (prior-year carryover input) | **P1** | Found during the 16 Jul 2026 "what does a tax pro actually buy" audit, verified by grep: `computeFtc` computes THIS year's excess Indian tax as `usCarryover` and reports it, but nothing ever accepts LAST year's unused credit back in — and the passive/general basket distinction appears only in human-readable trace prose ("Form 1116 general basket", "Passive basket"), never in the arithmetic: the §904 limitation is computed on one aggregate pool, so passive-heavy Indian income (interest/dividends/CG) can wrongly absorb limitation headroom created by general-basket wages and vice versa. Practitioner sources (IRS i1116; multiple expat-practice guides, checked 16 Jul 2026) confirm basket-locked carryovers (excess in one basket can never offset the other) plus Schedule B carryover reconciliation are among the most common Form 1116 errors. Current-year per-basket split is buildable now — the model already classifies every income stream by type; the carryover input needs a Layer 1 field (per-basket prior-year unused credit). Single-year-snapshot caveat: like India's `carryForwardLosses`, this would be an entered opening balance, not true multi-year tracking. |

| US-31 | **No filing-status / election comparators — the US twins of IN-36: MFJ-vs-MFS is never computed side-by-side (including the §6013(g) full-year-election variant for an NRA spouse, THE arrival-year decision for the corridor), and FEIE-vs-FTC-only is gated for eligibility but never compared for outcome** | ❌ | 🟢 Now | P2 | Found in the 16 Jul 2026 first-class symmetry sweep, prompted by asking what the US-side equivalent of IN-36 (regime comparison) is. The engine already handles MFS *within* a chosen status correctly everywhere it matters (SALT $20k/$250k MFS halving, tips/overtime denial, AMT break halving — all verified present), and FEIE has real eligibility gating plus the §911 no-double-dip — but neither choice is ever computed BOTH ways with a delta shown, even though `computeUsTax` is fully parameterized by filing status (same run-it-twice shape as IN-36) and the FEIE-vs-FTC choice is a genuine annual computation decision for every US-citizen-in-India profile (revoking FEIE carries a 5-year re-election bar — §911(e)(2) — so the comparator must carry that caveat, the mirror of IN-36's once-in-a-lifetime 10-IEA caution). Both are "show both sides of the auto-decision" (CL-5) instances with real dollar deltas. |

**Buildable-now count (US): 9 of 19 remaining (3 partial)** (US-1, US-2, US-5, US-16, US-17, US-19, US-20, US-21, US-26, US-27, US-28, US-29 shipped) — US-3 (partial), US-7, US-9, US-12, US-13, US-14, US-18 (partial), US-30 (partial — basket split now, carryover input needs field), US-31.

**Verified current / already modeled (US):** NIIT 3.8% ✓ · Additional Medicare 0.9% ✓ · SE tax ✓ · QBI §199A ✓ · AMT with ISO/PAB preferences ✓ · OBBBA SALT cap $40k with 30¢ phase-down ✓ · OBBBA senior deduction ✓ · OBBBA tips/overtime deductions ✓ · CTC $2,200 ✓ · Trump Account §530A cap ✓ · FBAR $10k and Form 8938 thresholds unchanged for 2025/2026 ✓.

---

## C. Cross-border lens (carried forward, verified 11 Jul 2026)

| ID | Item | Status | Build Now? | Priority | Detail |
|---|---|---|---|---|---|
| XB-1 | **US estate & gift tax exposure** | ❌ | 🟡 Needs field **(corrected 13 Jul 2026 — was 🟢)** | **P1** | No India-US estate treaty (income-tax DTAA only). NRA US-situs assets get a frozen **$60,000** exemption vs the OBBBA-permanent **$15M/$30M** (2026+, inflation-indexed 2027+) for citizens/residents; 40% top rate, zero treaty relief. No Form 706-NA / 709 / lifetime-gift tracking anywhere. **Field-verified during a Filings-tab coverage audit: the "Layer 1 already knows US brokerage/property values" claim below does not hold up.** `real_estate.properties[]` carries only `gross_rent_usd`/`expenses_usd` (rental income, not asset value/FMV); no US brokerage/portfolio value aggregate exists anywhere in the model (`financial_holdings` on the US side is read nowhere — see US-side dead-data note). An honest exposure estimate needs a real US-situs asset-value field first; flipped from 🟢 to 🟡 rather than ship a number built on rental income mistaken for asset value. |
| XB-2 | ~~No US-India totalization agreement~~ **Already shipped — status was stale, corrected 16 Jul 2026** | ✅ shipped | — | — | Found this row was WRONG while auditing "are there similar findings we're missing for the dual-jurisdiction profiles too?": a `no_totalization_agreement` finding already exists in `conflicts.js` (§4e) and fires correctly — confirmed live for `us_citizen_expat_india` (Grace Thomas, $8,159 US Schedule SE tax on $57,741 of foreign India self-employment earnings) and `us_resident_indian_income` (Rohan Mehta) before touching any code. This row's ❌ status simply never got updated when that finding shipped in an earlier session. No functional change — documentation correction only, so no new tests/verification beyond re-confirming the existing finding still fires. |
| XB-3 | Form 3520/3520-A penalty exposure (gifts > $100k individual / $20,573 corp 2026; 5%/mo to 25%) | 🟡 | 🟢 Now/partial | P2 | A qualitative flag (gift received above threshold, trust beneficiary) is buildable now from the existing boolean facts. Quantifying the actual 5%/month penalty needs the gift's dollar VALUE, which isn't captured as a field yet — that half is 🟡. |
| XB-4 | PFIC §1291/QEF/MTM computation (Form 8621, Dec-2025 revision) | 🟡 | 🟡 Needs field | P2 | Holdings tracked (name + value), but per-fund acquisition date, distributions received, and QEF/MTM election status aren't captured — all required for any of the three computation methods. Indian mutual funds ≈ PFICs — the most punitive common trap for US-person NRIs. |
| XB-5 | Forms 8865 / 8858 / 926 (foreign partnerships, DREs, transfers to foreign corps) | ❌ | 🟡 Needs field | P3 | Only 5471/PFIC entity data is captured today; foreign partnerships and DREs aren't modeled as entities at all. |
| XB-6 | Form 8854 exit tax (covered expatriate) | 🟡 | 🟡 Needs field **(corrected 13 Jul 2026 — was 🟢/partial)** | P2 | **Field-verified during a Filings-tab coverage audit: no per-taxpayer covered-expatriate field exists.** The only expatriation-adjacent field found is `foreign_gifts_and_trusts.received_gift_from_covered_expatriate` — whether the taxpayer *received a gift from someone else* who is a covered expatriate (drives the existing §2801 finding) — not the taxpayer's own GC-years-held / I-407 relinquishment date. Neither exists anywhere in `normalize.js`. Both the covered-expatriate flag itself and the mark-to-market computation are 🟡 Needs field, not just the computation half. |
| XB-7 | ~~Black Money Act 2015 exposure~~ **FIXED** | ✅ shipped | — | — | New `black_money_act_exposure` finding, fired alongside the existing `schedule_fa_inconsistent` (same trigger: an India ROR whose India form denies foreign assets while the US form shows real foreign holdings) — that finding only flagged the forms CONTRADICTION; this one quantifies the actual stakes: flat 30% tax (s.10) on the undisclosed asset value plus a penalty of up to 3x that tax (s.41, up to 90% of asset value) — up to 120% of asset value total — plus possible prosecution (s.51, up to 10 years' rigorous imprisonment), separate from and cheaper-to-cure via FAST-DS 2026 (XB-21, cross-referenced in the finding's own recommendation text). Asset value approximated from US brokerage/retirement holdings (`model.assets.usSecurities`) plus non-India bank accounts. Verified: 27-test suite; hand-checked exact against Aarav Sharma (`dual_resident_h1b`) — $260,000 undisclosed US financial holdings → $78,000 tax + up to $234,000 penalty = $312,000 total exposure, matching the formula to the dollar; confirmed it does NOT fire for any profile where foreign assets are correctly declared (no false positive). |
| XB-8 | ~~Transfer pricing (s.92) related-party flag~~ **FIXED** | ✅ shipped | — | — | Shipped as a `detectConflicts()` finding (14 Jul 2026) — not `buildScopeNotes()`, which was tried first and then corrected: scope notes render collapsed by default at the bottom of the Monitor page and don't feed the severity counts/badges/alerts, so a real compliance action item there is too easy to miss. Moved to a genuine `S.WARNING` finding (`add("transfer_pricing", ...)`, section 9b, right after the CFC/Form 5471 check) so it appears in the actual visible, filterable Conflicts panel and counts toward the Warning badge. Fires only when a real US-India related-party ownership link exists in the data (`assets.usOwns10PctForeignCorp` or `assets.usForeignCorps[]` — the same signal `form_5471`'s own trigger already trusts, not gated on `res.us.isResident` since s.92 binds the Indian entity's own return regardless of the US owner's personal residency) — verified `founder_indian_company`/`us_ccorp_indian_sub` show it, `india_pvt_ltd`/`foreign_holdco_poem_india` (business profiles with no US-side owner on file) correctly don't. `amountUsd: 0` and no ALP computation, by design — the finding's Action line points to Form 3CEB/Rule 10D/§482 and a specialist engagement rather than asserting a number WISING can't compute. |
| XB-9 | GAAR | 🚫 | ⚪ N/A | — | Facts-and-circumstances doctrine; not safely rule-encodable. Recorded as deliberate exclusion. |
| XB-10 | MLI | 🚫 verified non-issue | ⚪ N/A | — | US never signed the MLI → India-US treaty untouched. Nothing to build. |
| XB-11 | India-US DTAA | ✅ current | ⚪ N/A | — | No amendment since the 2000 protocol; as modeled. |
| XB-12 | W-9 / FATCA self-certification pair | 🟡 in flight | ⚪ N/A | P2 | Both Antigravity prompts issued (India bank-level self-cert; US-side W-9-on-file). Engine wiring pending the updated Layer 1 files. |
| XB-13 | India resident per-source TDS fields (194A/193/194/194DA/194-I) | 🟡 in flight | ⚪ N/A | P2 | Antigravity prompt issued; engine wiring pending. |
| XB-14 | **GILTI / Subpart F quantification — and OBBBA "NCTI" parameters if built** | 🟡 | 🟡 Needs field | P2 | Currently disclosure-only (Form 5471 flag; no dollar figure). Actual CFC financials (E&P, QBAI, tested income) aren't captured — needed before any quantification, whichever era's rules are used. If/when built it must use the OBBBA rules effective TY2026: renamed **NCTI**, §250 deduction 50%→40%, QBAI exclusion eliminated, FTC haircut 20%→10% — recorded now so a future build doesn't use pre-2026 parameters. Note (14 Jul 2026): ownership%/identity for a manually-entered foreign corp now correctly reach the engine (US-26 fixed); GILTI dollar amount still doesn't — the manual card has no GILTI input at all, which is this row's actual scope. |
| XB-15 | Chapter XII-A Layer 1 round-trip (`nri_exit_type` dropdown + `investment_income_this_year` field) | 🟡 in flight | ⚪ N/A | P2 | Antigravity prompt issued earlier in the project; engine side is already built and safely gated (computes nothing while the fields are absent). Waiting on the updated `layer1_india.html`. |
| XB-16 | Payer-side withholding-agent compliance (taxpayer AS deductor) | 🚫 recorded | ⚪ N/A | — | The s.40(a)(i)/(ia) disallowance fields already feed the business computation (expense denial for failing to deduct TDS on payments made). Deliberately excluded from the Withholding Taxes page, which covers tax withheld FROM the taxpayer's income — recording the decision so it isn't re-litigated. |
| XB-17 | FATCA Chapter 4 (§§1471-1474) withholding on payments to FFIs | 🚫 | ⚪ N/A | — | Institution-side withholding regime; individuals interact with it only via the self-certification items already tracked (XB-12). Out of scope by design. |
| XB-18 | ~~Monitor's "Residency Day-Counters" panel showed a day-count presence-test bar for EVERY taxpayer, including companies/HUFs/firms/US entity-type filers — none of which have a day-count residency test at all~~ **FIXED** | ✅ shipped | — | — | User-directed, spotted from a live screenshot of a foreign-holdco profile showing a fabricated "365/182 days" bar. Day-count (US Substantial Presence s.7701(b), India s.6(1) ≥182 days) is INDIVIDUAL-only; a company/HUF/firm/trust/etc has its own qualitative test. Worst case found: Sharma HUF (`sharma_huf`) — 0 days entered — showed "Non-resident, 182 days of headroom" directly under a Flag card correctly saying "ROR," an outright on-page contradiction, not just a misleading-but-consistent number. `monitoring.js`'s residency builder now branches by `model.entity.indiaKind`/`usIsBusiness`: individual keeps the existing day-count bar unchanged; India company shows incorporation-or-POEM facts (`model.companyResidency`, already read for the `entity_dual_residency_poem` finding but never surfaced here); India HUF/firm/LLP/trust/AOP/BOI/AJP/etc shows the s.6(2)/s.6(4) control-&-management fact (`residency_detail.is_wholly_outside_india`, collected by Layer 1's own `div-res-wholly-outside` question but previously read nowhere in the engine at all) — with an HUF-specific wrinkle preserved: once the HUF itself is resident, the KARTA's own day-count still determines ROR-vs-RNOR sub-status (s.6(6)(b)), which Layer 1 itself surfaces in exactly that case, so it's carried forward as a fact line rather than dropped; a US ccorp/scorp/partnership/trust shows place-of-organization/incorporation (`profile.incorporated_in_us`/`incorporation_state`, not previously exposed on the model outside one inline `businessEntities()` check). The Monitor overview table's "Days Present"/"Physical Presence" columns (`lib/wising.js`'s `countriesFromEngine`, a second independent surface with the same bug) now read from the same per-taxpayer entries instead of unconditionally re-deriving day-count, and `residencyPct()` (`lib/logic.js`) collapses an entity's qualitative resident/non-resident fact to 1/0 so the EXPOSED/APPROACHING/NEXUS KPI classification still means the right thing for an entity (a resident company had been landing in EXPOSED only by coincidence, via a fabricated day-count that happened to exceed the threshold — fixed to key off the real qualitative fact directly). Also added, same session: `entity_type`/`tax_entity_type` badges in the Monitor header (visible on every tab, not just Residency) so the taxpayer type is never ambiguous. Verified: 25-test suite, live screenshots across a foreign-incorporated company (Meridian), an HUF (Sharma HUF — confirmed the contradiction is gone), a US C-corp with an Indian company on both sides at once (Cloudspire Inc), and an unchanged individual/individual baseline (Rohan Mehta). |

| XB-19 | ~~The Monitor showed a fabricated DUAL-jurisdiction picture (both country badges, both residency Flag cards, a DTAA Treaty Position card) for taxpayers with zero real presence on one side~~ **FIXED** | ✅ shipped | — | — | User-directed, found immediately after XB-18 shipped: the header badge for Meridian Holdings (a Singapore company with POEM in India, zero US days/citizenship/green-card/US-source income) read "🇮🇳 Company / 🇺🇸 Individual"; a first pass mirrored India's label to get "🇮🇳 Company / 🇺🇸 Company", but the user correctly rejected that too — Meridian has no company (or anything else) in the US at all, and "how can US be an HUF?" for Sharma HUF made the same point even sharper. Root cause: Layer 1's `india`/`us` bundle always exists as a shell so neither form crashes if opened, and Router's own `jurisdiction` toggle (a real "Dual"/"India only"/"US only" selector on `router.html`) defaults to "dual" and was never overridden on any of the 9 demo profiles — so `model.meta.jurisdiction` read "dual" even for taxpayers with nothing on one side. Fixed by computing scope dynamically instead of trusting either signal: new `model.meta.hasIndiaScope`/`hasUsScope` derive from real reported facts (`router.us_days > 0`, `is_us_citizen`, `has_green_card`, `has_us_source_income_or_assets`) — Router's own explicit "India only"/"US only" choice still wins outright when a preparer deliberately sets it, but the default "dual" state is now checked against what was actually reported rather than trusted blindly. `model.meta.jurisdiction` itself is recomputed to this effective value so existing consumers (`taxComputation`/`summary`) stay correct too. The Header badge row, the two Residency Flag cards, the Residency Determination panel's entries, and the DTAA Treaty Position card (Article 4 tie-breaker / TRC / Form 10F / PE / 1040-NR are all meaningless without real dual exposure) now render only the in-scope country(ies) for a single-jurisdiction taxpayer, while the Residency & Treaty Conflicts panel stays (still surfaces genuine single-country findings). Verified: 25-test suite; live screenshots — Meridian and Sharma HUF now show India-only end-to-end (one header badge, one Flag card, one Residency Determination entry, no DTAA card, period string reads "TY2026-27 (India)" only); Cloudspire Inc (`us_ccorp_indian_sub` — a genuinely distinct US C-corp + Indian subsidiary, not a scope gap) and every dual individual profile (e.g. `dual_resident_h1b`) unchanged, still show both sides in full. |

| XB-20 | **Foreign retirement-account accrual/withdrawal timing mismatch — India's Section 158 (ITA 2025, successor to s.89A/Finance Act 2021) relief election, Form 40 (replaces Form 10EE)** | ❌ | 🟡 Needs field | **P1** | Verified via web research 16 Jul 2026 (not previously tracked anywhere in this doc). India taxes on accrual; the US taxes 401(k)/IRA growth only on withdrawal — absent an election, a returning NRI's retirement account is taxed in India every year on unrealized growth it hasn't touched, with no matching US tax paid that year to credit against it (FTC needs same-year, same-income tax paid in both countries — a structural mismatch, not something FTC alone fixes). Section 158 lets a "specified person" (India-resident who opened the account while NR-in-India/resident-in-the-notified-country) elect to defer Indian taxation on a "specified account" until the US itself taxes it (withdrawal/redemption) — filed via Form 40 by the return due date. Separately, DTAA Article 20 (private pensions) taxes *periodic* 401(k)/pension payments only in the country of residence, but Article 23 ("Other Income") governs *lump-sum* withdrawals, which both countries can tax — a real, taxpayer-facing classification decision this app doesn't currently model at all. Layer 1 US collects 401(k)/IRA/pension *contributions* in detail (`retirement_accounts.*`) but *distributions* (`ira_distributions_usd`, `401k_distributions_usd`) have no UI at all (see US-24) — and neither side has a field for the Section 158 election itself, the account's "specified account" status, or periodic-vs-lump-sum characterization. A real RNOR-aware disclosure finding (flag the exposure once RNOR ends and distributions exist, point to Form 40/Section 158 and the Article 20/23 distinction) is buildable once US-24 wires the distribution fields; the election/characterization fields themselves are a genuine Layer 1 build, not just an engine fix. |
| XB-21 | **FAST-DS 2026 (Foreign Assets of Small Taxpayers – Disclosure Scheme)** — a one-time 6-month amnesty window (commencement date not yet notified) for undisclosed foreign assets/income, relevant to every taxpayer already flagged by XB-7's Black Money Act exposure | ❌ | 🟢 Now (disclosure only) | P2 | Verified via web research 16 Jul 2026, Budget 2026 (Finance Bill 2026, Clauses 114-128). Two tracks: Category A (asset/income never taxed or reported) pays 60% (30% tax + 30% penalty) of value as of 31 Mar 2026; Category B (asset bought from already-taxed income, or acquired while genuinely NRI) pays a flat ₹1,00,000 fee — both buy immunity from further demand/penalty/prosecution under the Black Money Act. A supplementary immunity also exists for non-immovable foreign assets under ₹20L aggregate, independent of the scheme. Buildable now as a qualitative disclosure addendum to the existing XB-7 finding (same trigger facts — Schedule FA-relevant foreign holdings), not a new data requirement; commencement date being unnotified means don't hardcode a filing deadline yet. |
| XB-22 | **OBBBA 1% federal excise tax on cash-funded remittances abroad** (effective 1 Jan 2026) | ❌ | 🟡 Needs field | P3 | Verified via web research 16 Jul 2026. A new US-side sender tax — 1% of the transfer amount, collected by the remittance provider — but applies ONLY to transfers funded by physical cash, money order, or cashier's check; transfers funded from a US bank account, debit card, or credit card are exempt. Real-world hit rate for this app's typical NRI-to-India wire/ACH transfers is likely low (most flow through bank-funded channels, not cash), but it's a genuine new 2026 US tax with zero current field to capture "was this outbound transfer cash-funded" — distinct from India's own LRS outbound-remittance tracking (`lrs_outbound`, which governs money leaving INDIA, not money leaving the US). Lower priority than XB-20/XB-21 given the narrow cash-funded scope, but flagged so it isn't missed if a real cash-remittance profile shows up. |

| XB-23 | ~~Several cross-border findings (FTC shortfall, Form 44, FBAR, tax-year apportionment, FX-basis, 1099-DA awareness) and the world-map jurisdiction count fired regardless of whether the taxpayer actually had exposure in the second country~~ **FIXED** | ✅ shipped | — | — | Found immediately after building the `india_only_ca_client`/`us_only_cpa_client` demo profiles (single-jurisdiction, no US/India exposure at all): a purely domestic Indian resident was shown a CRITICAL "Foreign Tax Credit shortfall — residual double taxation" (there is no double taxation when only one country ever taxed her), a CRITICAL "FBAR threshold breached" (she is not a US person at all), and an INFO "Form 44 required" / "check for US Form 1099-DA" / "Tax-year apportionment" / "FX conversion basis" — none applicable to a single-country taxpayer. Root causes, both distinct from XB-19 (which only fixed the Monitor's residency-panel DISPLAY, not the underlying finding-generation logic): (1) `conflicts.js`'s finding gates used the structural `model.meta.hasIndia`/`hasUs` (true whenever Layer 1's shell object exists, i.e. always) instead of the data-driven `hasIndiaScope`/`hasUsScope` (XB-19) — fixed across 9 call sites (`conflicts.js` FORM 2210, 1099-DA awareness, FTC gap/available, Form 44, tax-year apportionment, FX basis, `form_26as_ais_tis`/`form_16_16a`/`form_4868` document flags, `buildScopeNotes`'s `dual` gate; `computation.js`'s `indiaItrForm` solver gate); (2) `aggregateAccounts` (`normalize.js`) unconditionally summed a taxpayer's own bank accounts into the "FBAR aggregate peak" whenever Layer 1 US never separately computed one — correct for a genuine dual taxpayer (their India accounts ARE foreign-to-them-as-a-US-person) but wrong for a taxpayer with no US nexus at all, whose own domestic accounts aren't "foreign" to anyone; now takes a `hasUsScope` parameter and returns $0 when false; (3) `countriesFromEngine` (`lib/wising.js`) always returned both India AND US country objects, so an out-of-scope country rendered as a green "On track" tracked jurisdiction (and counted toward "ALL JURISDICTIONS") instead of the map's own pre-existing "Not tracked" gray default for any country with no status entry — now omits an out-of-scope country entirely, letting it fall through to that existing treatment for free. Verified: 27-test suite (up from 25 — the 2 new demo profiles feed the same smoke-test loop automatically); both new profiles hand-checked to show zero cross-border findings and a correct 1-jurisdiction map; all 6 existing dual-jurisdiction profiles hand-checked to show their FTC/Form-44/apportionment/FX/FBAR findings unchanged (no regression). |

| XB-24 | ~~The "FTC Reconciliation" card showed a nonzero "Net Unrelieved Double Tax" for a taxpayer with zero exposure in the second country — the same class of bug as XB-23, in a third, separate location it didn't reach~~ **FIXED** | ✅ shipped | — | — | Found immediately after XB-23 shipped, while visually re-checking the Reconciliation tab (not just the Conflicts panel) for `india_only_ca_client`: the card showed **$48,987** in red "Net Unrelieved Double Tax" for a taxpayer with zero US exposure at all — the exact wrong claim XB-23 had just fixed in the Conflicts-panel finding, but this card is fed by `computeFtc()` (`computation.js`) directly, a genuinely separate code path from the `detectConflicts()` finding XB-23 gated. Root cause: `computeFtc()` only zeroed the US-direction credit computation for `usTax.isNra` (Form 1040-NR filers) — a narrower condition than "has no US exposure at all"; a taxpayer who isn't a US taxpayer in ANY sense (not even NRA, since no US return is filed at all) fell through to the "normal dual taxpayer" branch, which unconditionally treated 100% of her India income as "foreign-source relative to a US return." Fixed by extending the SAME zero-out the code already used for NRA to also cover `!model.meta.hasUsScope`, in **two** places — the initial `foreignSrcGrossUsd` assignment, and (caught on the first verification pass, not guessed) the separate `creditableFraction` fallback a few lines below, whose own code comment already explicitly warned this exact `: 1` fallback would "wrongly multiply a real India tax figure by 1" if the numerator were zeroed without also gating the fallback — fixing only the first line reproduced precisely the bug that comment described. Verified: 27-test suite; `computed.ftc.netUnrelievedDoubleTaxUsd` confirmed $0 for both `india_only_ca_client` and `us_only_cpa_client` (previously $48,987/already-correct-$0 respectively), unchanged (still nonzero, real figures) for all dual-jurisdiction profiles; live screenshot of the Reconciliation tab. |

| XB-25 | **Dual-status year (the move year itself) not modeled — an arrival/departure-year taxpayer is computed as a full-year single-status filer; Layer 1's own dual-status fields are collected but never read** | ❌ | 🟢 Now (disclosure/eligibility half) / 🟡 (split-year computation) | **P1** | Found during a user-prompted "is cross-border coverage really enough?" audit (16 Jul 2026). The move year is arguably the single most common cross-border engagement — every new H-1B arrival and every returning NRI has exactly one — and it's the one scenario the engine can't represent: the US residency solver only ever produces a full-year status, and `constants.js` defines `RESIDENCY.DUAL_STATUS` that NOTHING ever assigns (dead constant since it was written). Meanwhile `layer1_us.html` already collects the key facts — `dual_status_arrival_date` (`#res-dual-arrival`), `first_year_choice_entry_date`/`first_year_choice_election` (§7701(b)(4) First-Year Choice), and `s6013g_joint_election` (full-year-resident MFJ election for a dual-status spouse) — and `normalize.js` reads NONE of them (verified by grep: the only §6013 field the engine reads is the separate `nra_specific.s6013h_joint_election`); same "collected but never read" class as IN-28/FIELD_COVERAGE_AUDIT. Real consequences of dual-status the engine currently gets silently wrong for a move-year filer: **no standard deduction** (dual-status filers must itemize — the engine hands them the full standard deduction today), **no MFJ / no joint standard deduction** absent a §6013(g)/(h) election, nonresident-period income taxed only if US-source (1040-NR statement) vs resident-period worldwide — and the §6013(g) full-year-MFJ election is a genuine computation trade-off (joint rates + standard deduction vs worldwide taxation of the pre-arrival period) a corridor pro decides every arrival year. India side needs NO change (s.6 status genuinely applies to the whole FY — no split-year concept in Indian domestic law; the existing FY/CY apportionment already handles the calendar mismatch). Buildable NOW from existing fields: a disclosure finding (dual-status filing consequences + First-Year-Choice/§6013(g) election framing) and the standard-deduction/MFJ eligibility gates. Needs more: the actual split-year computation requires income split at the residency-start date, which Layer 1 only captures annually (the India side's quarterly structure doesn't cover US-source income). |

| XB-26 | **Roth accounts — Layer 1 US collects Roth IRA/401(k) contributions and a backdoor-Roth flag; the engine reads NONE of it, and the India side of the trap (India does not recognize Roth's tax-free character) is unmodeled** | ❌ | 🟢 Now (disclosure) / 🟡 Needs field (computation) | P2 | Found during the 16 Jul 2026 "what does a tax pro actually buy" audit, verified by grep: `roth_ira_contribution_usd`, `roth_401k_contribution_usd`, `backdoor_roth_executed` exist only in layer1_us.html — zero engine references (same "collected but never read" class as IN-28/XB-25). Two distinct real issues: (1) US-side — a backdoor Roth without checking the §408(d)(2) pro-rata rule (existing pre-tax IRA balances make part of the conversion taxable, Form 8606) is a classic self-inflicted wound, and the direct-contribution MAGI phase-out is checkable from AGI already in the model; (2) India-side — for a returning NRI who becomes ROR, India taxes Roth earnings as ordinary investment income (no Indian recognition of the US tax-free wrapper; and s.158/Form 40 relief — XB-20 — is framed around tax-DEFERRED accounts whose US tax comes at withdrawal, a poor fit for Roth where US tax already happened at contribution — making Roth arguably WORSE than a 401(k) to carry back to India, the opposite of its US-side reputation). A disclosure finding for both is buildable from existing fields; quantification needs balances/distributions (the same missing fields as US-24/XB-20). |
| XB-27 | **NRE vs NRO account-type distinction absent — the most-cited practitioner NRI trap (NRE FD interest exempt in India but fully taxable in the US) cannot even be represented in the model** | ❌ | 🟡 Needs field | P2 | Same audit. Practitioner checklists (checked 16 Jul 2026) consistently rank "NRE interest is tax-free in India but must appear on the 1040 as interest income" among the top mistakes CPAs catch in NRI returns, alongside its mirror (NRO interest carries 30%-plus-surcharge TDS u/s 195 that the US return should be claiming as FTC). Layer 1 India's interest fields (`interest_savings_inr`, `interest_fd_rd_inr`) carry no NRE/NRO/resident account-type tag — the only NRE/NRO structures that exist anywhere are `nro_repatriation` (itself unread, IN-30) and an s.197 lower-TDS-certificate NRO_INTEREST checkbox (also unread, IN-16) — so the engine cannot (a) exempt NRE interest India-side for an NR/RNOR, (b) flag it as US-taxable-anyway for a US person, or (c) apply the NRO TDS expectation to the withholding reconciliation. Needs a per-account or per-stream account-type field; once tagged, all three consequences are engine-ready (the US-side worldwide-interest path already exists). |

| XB-28 | ~~The Compliance Calendar and the Limit Monitoring gauges showed BOTH jurisdictions' entries regardless of scope — a US-only client saw India advance-tax quarters and ITR deadlines; an India-only client (including all three India-only entity profiles) saw US estimated-tax dates, Form 1040 deadlines, and $0 FBAR/Form 8938 gauges~~ **FIXED** | ✅ shipped | — | — | The scope-blindness class (XB-19 display / XB-23 findings+FBAR+map / XB-24 computeFtc) in a FOURTH and FIFTH location, found 16 Jul 2026 while verifying the s.425 presumptive-proviso fix across profiles: `monitoring.js`'s deadline builder had no scope check at all (and the UI renders `calendar.all` unfiltered), and `computation.js`'s `computeLimits` built the FBAR/8938 gauges unconditionally (an India-only taxpayer seeing an FBAR gauge — even at $0 — reads as "this tool thinks I'm a US filer", a guaranteed known-answer-test failure for a CA) and the LRS/NRO gauges unconditionally (mirror problem for a US-only client). Fixed with the same `model.meta.hasIndiaScope`/`hasUsScope` gates as the rest of the class: calendar rows filtered by `jur`, FBAR/8938 gauges gated on US scope, LRS/NRO on India scope (FEIE/§530A gauges already self-gate on real US-side facts). Verified: 27-test suite; all 11 profiles checked — Kavya/David/Nimbus/Meridian/Sharma HUF now show only their own jurisdiction's deadlines and gauges, Grace Thomas additionally shows the single presumptive advance-tax installment, every dual profile unchanged (6 IN + 6 US deadlines, full gauge set). |

**Buildable-now count (cross-border): 4 of 20 remaining (3 partial)** — XB-3 (partial), XB-21, XB-25 (partial — disclosure/eligibility half now, split-year computation needs per-period income), XB-26 (partial — disclosure now, computation needs balance/distribution fields). **Correction (D-rollup pass, 16 Jul 2026):** XB-1 and XB-6 were miscounted into this bucket — both show 🟡 **Needs field** today (their Build-Now cells carry a "(corrected 13 Jul 2026 — was 🟢...)" historical note that a prior pass misread as a live 🟢). Both stay in the 🟡 bucket below; corrected count was 6, is now 4.

---

## D. Buildability summary

**Recomputed 16 Jul 2026** by mechanically parsing every row's own Status/Build-Now cells (not hand-counted) across all 95 jurisdiction-scoped rows (IN-1..36, US-1..31, XB-1..28). This pass corrected two real errors: (1) the cross-border "Buildable-now" summary line had miscounted XB-1 and XB-6 as 🟢 — both carry a "(corrected 13 Jul 2026 — was 🟢...)" historical note in their Build-Now cell that a prior pass misread as a live status; both are genuinely 🟡 today and are counted correctly below. (2) US-29 was filed out of numeric order (between US-18 and US-19) from an earlier insertion; moved to its correct position after US-28. Six further rows (CL-1..5, PD-1 — the confirmatory-layer and practice-deliverable items from sections F/G) sit outside this table by design: they aren't jurisdiction-scoped gaps, all six are 🟢 buildable now, and none are shipped yet.

| Bucket | India | US | Cross-border | Total |
|---|---|---|---|---|
| 🟢 Buildable now, not yet done | 8 | 9 (3 partial) | 4 (3 partial) | **21** |
| ✅ Shipped | 14 | 12 | 8 | **34** |
| 🟡 Blocked on a new Layer 1 field / build | 9 | 9 | 8 | **26** |
| ⚪ N/A (in-flight, recorded, or verify-first) | 5 | 1 | 8 | **14** |
| **Total** | 36 | 31 | 28 | **95** |

Reading this: **34 of 95** rows are shipped. Of the **61 open** rows, **21 need zero Layer 1 changes** (engine/UI work only — this is the queue to work from next; see section E), **26 are genuinely blocked** on a new field or larger build (several already have Antigravity prompts issued — see XB-12/13/15), and **14** are already-correct/recorded-decision/verify-first items that don't need a build either way. The "✅ Shipped" bucket is a straight count of every row currently marked shipped in its Status cell — it does not attempt to reconstruct which were originally 🟢 vs 🟡 before being built, since that history isn't reliably reconstructable from the document alone for older rows.

---

## E. Suggested build order (P1s first, drawing only from the 🟢 Now bucket)

**Rebuilt 16 Jul 2026** against section D's corrected count. The prior version of this list had gone stale in the same way D had: it named US-2 and XB-2 as still-to-build (both shipped since), XB-1 as a quick win citing "US-situs asset values already known" (the row was corrected 13 Jul to say the opposite — no such field exists — which demoted it out of the 🟢 bucket entirely), and carried a duplicate "9." Every item below is drawn fresh from D's current 21-row jurisdiction-scoped 🟢 bucket plus the 6 non-jurisdiction items in sections F/G (CL-1..5, PD-1) — 27 buildable-now items total, none shipped yet, grouped by natural build-together clusters rather than a flat priority sort.

**Already shipped, for reference (not part of this list):** Phase 0 (IN-21+US-16), IN-1+US-1 (advance-tax/estimated-tax interest), US-2 (SS taxability), XB-2 (totalization, found already-built), IN-22..25 (India Phase 1), IN-15 (winnings TDS), US-29 (MACRS depreciation core), and everything else marked ✅ in sections A-C.

1. **CL-1 (checks-run registry)** — build first: it's the structural fix for the "known-answer test" a pro runs before trusting anything else, and both CL-3 (falls out of the same registry per its own row) and PD-1 (client report needs a "checked and clean" surface, not just failures) are cheaper once this exists.
2. **CL-2 (forward-looking calendar amounts)** — pure reuse of IN-1/US-1's already-shipped interest math run prospectively instead of retrospectively; the single most-pulled number per client per quarter.
3. **IN-36 + US-31 + CL-5 together** — one "comparator" feature: regime (India), filing-status/FEIE-vs-FTC (US), and standard-vs-itemized (the CL-5 general case) are the same "run the computation twice, show the delta" pattern, all P1/P2, all zero new fields.
4. **US-30 (FTC carryover + basket split)** — P1, standalone Form 1116 correctness fix; current-year basket split is the 🟢 half, carryover input stays 🟡 until a field is added.
5. **XB-25 (dual-status year, disclosure/eligibility half)** — P1, standalone; the arrival-year scenario this corridor sees most often.
6. **IN-4 (property-sale CG verification)** — P1, "audit then close whichever half is missing" rather than a from-scratch build; the highest-uncertainty item on this list, do it early to find out how much work actually remains.
7. **CL-3 + CL-4** — safe-harbor-met confirmations and elective-headroom gauges, same UI surface as items 1-2, cheap once the registry exists.
8. **PD-1 (client-ready branded export)** — sequenced after CL-1 per its own row's note; the first client-facing deliverable.
9. **XB-3 + XB-21 + XB-26** — three disclosure-only cross-border findings (Form 3520/3520-A exposure, FAST-DS 2026 amnesty framing, Roth account disclosure), same shape as the already-shipped XB-7/XB-8/XB-27-class findings.
10. **US-3 + US-9 + US-13 + US-14** — Withholding-tab depth: capital-loss carryover, Form 1116 basket/HTKO detail, FICA visibility, excess-SS credit — all read from data already driving the Withholding page.
11. **US-18 (remaining MACRS scope)** — extend the already-shipped US-29 depreciation engine to K-1/1120 asset rows and `farming_schedule_f[]`, the two array types deliberately left out of the original build.
12. **IN-6 + IN-8 + IN-16 + IN-30** — remaining India engine-depth items (presumptive-scheme limit tests, house-property caps, lower-TDS-certificate consumption, NRO-repatriation gauge) — verification/depth work against data already captured.
13. **US-12 (state income tax)** — flagged "large" in its own row; needs a scoping decision (which states beyond CA/NY already shipped) before estimating, so sequenced after the smaller wins above.
14. **P3 items as capacity allows**: IN-12 (s.194P senior disclosure), IN-26 (s.44BBB/35AD/115V, small population), US-7 (Form 1099-DA awareness).
15. **🟡 items as Layer 1 round-trips return** (XB-12/13/15 already have prompts issued; US-6, IN-3/17-19 need prompts written) — outside the 🟢 bucket, listed here only as the next queue once a field lands.
16. **Business-entity Phases 2-6** (entity graph, inter-entity flow edges, entity-switcher frontend) per `docs/BUSINESS_ENTITY_ARCHITECTURE.md` §5, once the queue above is solid.

## F. Confirmatory (green-state) layer — recorded 16 Jul 2026

Distinct from every gap above: these aren't missing detections, they're missing **confirmations**. A pro evaluating the product first runs known-answer tests — clients whose facts they already understand — and checks whether the product shows what they'd have concluded themselves; only after that trust is earned do the novel findings (the conflicts they DIDN'T know) carry weight. Today the engine evaluates dozens of gates silently and speaks only on failure, so "checked and correctly not required" is indistinguishable from "never checked" — the worst possible answer to a known-answer test. What already serves this layer well (verified 16 Jul 2026): both countries' full computation waterfalls with click-to-expand traces, the loss-set-off table, withholding expected-vs-actual rows, residency/ITR-form/FTC determinations with facts, six limit gauges (FBAR/8938/LRS/NRO-repatriation/FEIE/§530A) with pace projections, form-aware compliance calendar, and the "deliberately out of scope" honesty card.

| ID | Item | Build Now? | Priority | Detail |
|---|---|---|---|---|
| CL-1 | **Silent passes — a "checks run" registry** | 🟢 Now | **P1** | The structural fix for the known-answer test. Every finding gate that evaluates FALSE currently vanishes; instead each should emit a passed/not-applicable entry with the threshold and the client's actual figure ("FBAR: not required — aggregate peak $8,200 < $10,000 threshold" / "Wash-sale-style 26AS mismatch: checked, none found"). Rendered as a collapsed "✓ N checks passed" panel under the Conflicts list. Doubles as the pro's workpaper/malpractice-defense artifact: documented evidence the check was performed, not merely not-flagged. Zero new tax logic — the gates all exist; this is capturing their false branches. |
| CL-2 | **Forward-looking amounts on the calendar, not just dates** | 🟢 Now | **P1** | The advance-tax calendar entries carry the statutory percentages (15/45/75/100%) but no ₹ figure; IN-1/US-1 already compute total liability and per-installment requirements — retrospectively, for interest. Reusing the same math prospectively gives "Q3 installment due 15 Dec: ₹4.8L to stay penalty-free" and the 1040-ES/safe-harbor equivalent. This is the single most-used number a pro pulls per client per quarter. |
| CL-3 | **Safe-harbor-met / relief-applied confirmations** | 🟢 Now | P2 | IN-1/US-1 fire only on shortfall. When the client is SAFE, say so and why ("110% prior-year safe harbor met — no §6654 exposure regardless of current-year income spike"). Same class: marginal relief applied, 87A/§156 rebate applied, MAT not applicable and why. Some of this already surfaces inside computation traces; the confirmations belong at finding-level visibility, not buried in a waterfall expand. Largely falls out of CL-1's registry. |
| CL-4 | **Elective headroom gauges (80C/80CCD(1B)/80D remaining; 401(k)/IRA/HSA room)** | 🟢 Now | P2 | The gauge infrastructure exists (six gauges shipped); these are the missing ones pros check before every year-end: deterministic "you've used ₹1.1L of the ₹1.5L 80C cap — ₹40k of room left, old regime only" facts, not planning advice. Contribution figures are already collected on both Layer 1 forms; caps are already in constants.js (deduction caps) or trivially added (US elective limits). |
| CL-5 | **Show both sides of every auto-decision** | 🟢 Now | P2 | The US computation auto-picks max(standard, itemized) but displays only the winner; show the loser's figure and the delta ("itemized $31,400 vs standard $30,000 — itemizing saves ~$336"). IN-36 (regime comparison) is the same class on the India side and the highest-value instance. A pro checking the product's judgment needs to see that the alternative was computed, not just that an answer came out. |

## G. Practice-deliverable layer — recorded 16 Jul 2026

Distinct from sections A-F: not a detection gap and not a confirmation gap — a missing OUTPUT FORMAT for what the engine already computes. Prompted by a direct product question ("will tax pros actually pay for this, even single-jurisdiction?"): today every finding, waterfall, and reconciliation renders only inside the Monitor, which the pro looks at — the client never sees anything, so the product has no artifact that starts a billable advisory conversation. Explicitly scoped OUT of this: tax PLANNING (recommendations, strategy, "you should elect X") — deliberately deferred by product decision, not a gap, since an eventual AI layer is expected to do that better than hardcoded rules, and hardcoded planning logic ages badly against every Finance Act / OBBBA-style act anyway. This section is reporting only: a templated, client-facing rendering of figures the engine has already computed and verified, the same "state the current position" register as a lab report, not a treatment plan.

| ID | Item | Build Now? | Priority | Detail |
|---|---|---|---|---|
| PD-1 | **Client-ready branded export (PDF) — no client-facing output exists at all** | 🟢 Now | P2 | Every number needed already exists and is verified: exposure findings with dollar/rupee amounts (IN-1/US-1 interest, XB-7 Black Money Act exposure, FTC shortfall), the FTC reconciliation waterfall, filing deadlines. A templated PDF/print view assembling existing `findings`/`computed`/`monitoring` output under the firm's own branding is pure rendering — zero new tax logic, same category of work as the CL-section items. Market-validated as a real differentiator (2026 tax-planning-software research, not just an internal guess): "a professionally formatted plan with executive summary and roadmap justifies advisory pricing" and "the best platforms produce branded PDF reports CPAs deliver directly to clients" are cited as concrete purchase drivers, not planning-software fluff. Deliberately the reporting layer, not planning — see section intro. Sequencing note: naturally consumes CL-1's checks-run registry once built (a client report that shows only failures reads as alarmist; one that also shows what was checked and passed reads as thorough). |

## Maintenance

- Re-verify all "verified current" claims after: every Union Budget (Feb), every Finance Act notification (Mar), US filing-season changes (Jan), and any OBBBA technical corrections.
- When an item ships, move it to the relevant "verified current / already modeled" list with the commit hash rather than deleting the row.
- When a 🟡 row gets its Layer 1 field (prompt sent, file returned, engine wired), flip it to 🟢-done and note the commit, same as any other status change.
